# gitleaks finding fingerprints suppressed as verified false positives.
#
# Format: <commit>:<path>:<rule-id>:<line>
#
# This file suppresses ONE finding each, pinned to the exact commit, file, rule
# and line. It is deliberately not a path or rule allowlist: every rule still
# runs everywhere, and a real secret on an adjacent line is still caught.
#
# Prefer an inline `gitleaks:allow` comment for new code. A fingerprint is only
# needed when the finding lives in a commit diff that already exists in history,
# because a comment added later cannot change what an old commit's diff says.

# pg_dump's `--restrict-key` flag, given a fixed 32-character hex dummy, in the
# schema-dump test helper. It is a pg_dump 18+ option, not a credential, and the
# value is fixed on purpose: without it pg_dump emits a RANDOM \restrict key per
# run, so two dumps of identical schemas would differ and the convergence
# assertion (`assert_eq!(upgrade_dump, fresh_dump)`) could never hold. Matched by
# the low-confidence `generic-api-key` rule on the `key=<hex>` shape alone.
#
# Do not quote the literal value in this file. Doing so once put the same string
# on an added line of a NEW commit, which the scanner then flagged in turn --
# this file is scanned like any other.
# refreshed after the 2026-08-22 history rewrite invalidated the old commit SHA
4212e15f156251374715d356bcf698b4238ef345:hotel-app-be/tests/postgres_patch_lifecycle.rs:generic-api-key:423

# The same non-secret, quoted verbatim in this file's own comment by commit
# c4afdd1ca and flagged there in turn. That commit is already in history, so the
# reworded comment cannot unmake its diff; only a fingerprint can. See the note
# above about never quoting a flagged value here.
# refreshed after the 2026-08-22 history rewrite invalidated the old commit SHA
e4c5b0af2ee69bb9808dadf2b0eb84b0968118fe:.gitleaksignore:generic-api-key:13

# Post-rewrite refresh + known false positives (CI service creds, test
# fixtures, and placeholder ocids) re-surfaced when history SHAs changed.
1e91ef93fc8c836ebe1c498ce6cfb8339208e2fd:hotel-app-be/tests/communications_scheduler.rs:generic-api-key:24
df6f815e5b3bf9d41072dc71b7ed9d3172726ac0:infra/terraform/oci/environments/dev/terraform.tfvars.example:generic-api-key:11
df6f815e5b3bf9d41072dc71b7ed9d3172726ac0:infra/terraform/oci/environments/dev/terraform.tfvars.example:generic-api-key:12
0dedfef0e46a37a58a0d1e19acad6ce8d74e487f:.github/workflows/ci.yml:generic-api-key:123
# 0f1ce5a2 test fixture: dummy JWT_SECRET for the pre-arrival reminder suite.
# Value is a placeholder replaced by a low-entropy constant in a later commit;
# the pushed commit cannot be rewritten, so pin its fingerprint.
0f1ce5a2adf06e25a06824f01735b04467694125:hotel-app-be/tests/pre_arrival_reminder.rs:generic-api-key:33
# d5e707cc planning-doc code sample: dummy JWT_SECRET placeholder
# ('drift-test-secret-0123456789abcdef0123'), shown beside an equally fake
# postgres://drift:nopass@127.0.0.1:1/drift DSN. Not a credential; it exists
# only in that doc. The commit is pushed and cannot be rewritten, so pin it.
d5e707cc5d4b03bdbef5735cac01e393095faeb1:docs/superpowers/plans/2026-08-25-quality-enhancement-phase2.md:generic-api-key:72
