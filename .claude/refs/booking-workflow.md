# Booking Workflow (reference)

Rewritten 2026-07-12 (13-agent scan, independent verifier confirmed). Line anchors
verified by direct grep against this worktree on that date — verify again with
`grep -n "fn <name>" hotel-app-be/src/repositories/bookings/lifecycle.rs` before
relying on them; anchors rot as code moves.

**Three-layer architecture** (this is the load-bearing fact — previous version of
this doc described a two-layer handlers-only design that no longer exists):
- `routes/bookings.rs` — RBAC gate (`require_permission_helper`) + thin dispatch to `handlers::bookings::*`.
- `handlers/bookings.rs` (265 lines) — thin wrappers; most just destructure params and call `services::bookings` or `repositories::bookings` directly. No business logic.
- `services/bookings.rs` (536 lines) — holds real logic for **`void_booking`** (line 74) and **`manual_checkin`** (line 176): permission/ownership checks beyond the route-level RBAC gate, then delegates into `repositories::bookings`. Other handlers bypass this layer and call `repositories::bookings` (re-exported as `booking_repo`) directly.
- `repositories/bookings/lifecycle.rs` (4255 lines) — where almost all booking business logic actually lives: room locking, rate calculation, daily_rates rebuild, checkout accounting, city-ledger posting.
- `repositories/bookings/{checkin_advisory,complimentary,credits}.rs` — split-out sub-areas (checkin advisories, complimentary stays, guest credits); not covered in detail here.

**Routes** (`hotel-app-be/src/routes/bookings.rs`) — gated by `bookings:<read|create|update|delete|manage>` per-route via `require_permission_helper` (e.g. `delete_booking` requires `bookings:delete`, `void_booking`/`manual_checkin` require `bookings:update`); `my-bookings`, and code lookups (`/rate-codes`, `/market-codes`) are auth-only/public. Lifecycle status: `pending` → `confirmed` → `checked_in`/`auto_checked_in` → `checked_out`/`completed`; off-paths `voided`, `late_checkout`.

Key logic in `repositories/bookings/lifecycle.rs`:

- **`create_booking_handler`** (lifecycle.rs:1181) — opens a tx, locks the room with `SELECT … FOR UPDATE OF r`, checks for overlapping active bookings (`reserved`, `confirmed`, `checked_in`, `auto_checked_in`, `pending`, excluding `voided`), computes `is_tourist`/tourism tax from the guest (not trusted from the request — result is currently unused post-computation, see line 1312), computes `subtotal` from `daily_rates` if present otherwise `room_rate × nights`, inserts with status `'confirmed'` (hardcoded in the INSERT), sets the room `reserved`/`reserved_dirty` (dirty/cleaning rooms get the `_dirty` suffix), and optionally records a deposit `payment` row via `record_checkin_payment_tx` inside the same tx before committing.
- **`update_booking_handler`** (lifecycle.rs:1613) — RBAC + room/date conflict re-check; when dates change and no explicit `daily_rates` payload is supplied, **rebuilds `daily_rates`** (lifecycle.rs:1826) to span the new `[check_in, check_out)` range, preserving existing per-night values keyed by date and filling new nights with the booking's `room_rate` (without this, shrinking leaves orphan keys → over-charge; extending leaves missing keys → under-charge). Before a `checked_out`/`completed` transition, **`ensure_checkout_balance_resolved`** (lifecycle.rs:981) blocks checkout with balance due unless the booking is company-billed. On the transition itself: marks room `dirty`, generates an invoice via `services::payments::ensure_invoice_for_booking` (best-effort — failure is logged, not fatal), and if company-billed calls **`auto_post_company_ledger`**. Also **syncs existing `customer_ledgers.amount` by delta** (lifecycle.rs:2370) when the booking total changes on a non-checkout edit (preserves user-added extras; only touches `pending`/`partial` `room_charge` rows, skips paid/void).
- **`auto_post_company_ledger`** (lifecycle.rs:685, private fn) — called from inside `update_booking_handler`'s checkout branch (lifecycle.rs:2321), NOT inside its own transaction. Idempotency is enforced by an explicit `SELECT EXISTS(...)` pre-check (non-reversal `room_charge` row for the booking) rather than the DB constraint alone — if a row already exists, returns `Ok(())` immediately. `due_date` resolves via `default_payment_terms_days` settings-cache lookup, then the company's `payment_terms_days` overrides it if set. Reuses the booking's existing `invoices.invoice_number` if present (queried from the `invoices` table, not `customer_ledgers`) so a single booking has one invoice number across both tables. The partial unique index `uq_customer_ledgers_booking_room_charge` (PostgreSQL: `database/postgres/migrations/0001_v1_baseline.sql`; SQLite: `database/sqlite/migrations/0001_v1_baseline.sql`) is the structural backstop if the pre-check ever races.
- **`manual_checkin_handler`** (lifecycle.rs:2716) — called via `services::bookings::manual_checkin` (services/bookings.rs:176), which does its own permission check (`bookings:update`/`bookings:manage`, OR the booking's creator) before delegating to `checkin_booking_flow_for_booking`. Sets `checked_in` + `actual_check_in`, records optional deposit/payment, sets room `occupied`.
- **`delete_booking_handler`** (lifecycle.rs:2492) — called via `services::bookings::void_booking` (services/bookings.rs:74), which checks `bookings:update`/`bookings:delete`/`bookings:manage` OR booking ownership. Soft-void inside a tx: status → `voided` (`void_booking_tx`), frees the room (`release_room_tx`), cancels linked payments (`void_booking_payments_tx`, so they don't appear in night audit), and refunds complimentary nights into `guest_complimentary_credits` (`restore_complimentary_credits_tx`).

**Frontend**: `hotel-web-fe/src/api/bookings.service.ts` (`BookingsService`) wraps the endpoints; `features/bookings/hooks/useBookings.ts` composes TanStack Query hooks from `useBookingQueries.ts` (`useBookingsPage` uses `placeholderData: keepPreviousData` — this replaced the older manual request-id-guard pattern) with debounced text inputs (700ms via `useDebouncedValue`); `features/bookings/components/Bookings/BookingsPage.tsx` (2703 lines) orchestrates UI, delegating state to `features/bookings/hooks/useBookingsPageState.ts` (`handleConfirmCheckout` at line 249 just sends `updateBooking({status:'checked_out'})` — the backend handles invoice creation and company-ledger auto-post). `features/invoices/hooks/useCheckoutFlow.ts` is a shared checkout flow also used by `CustomerLedgerPage`.
