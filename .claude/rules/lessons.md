# Lessons Log

Append-only log of corrections and escalations. Format and triggers are defined
in `maintenance.md`. Newest at the bottom. Consolidate at >30 entries / >300 lines.

## 2026-07-05 — CLAUDE.md claimed desktop mode uses wildcard CORS
- Trigger: subagent scan of hotel-desktop/src-tauri/src/commands.rs:113-116
- Wrong: CLAUDE.md said `ALLOWED_ORIGINS=*` is "used by desktop mode"
- Right: desktop mode sets a specific origin list (`tauri://localhost,...`); the
  wildcard path exists in the backend but desktop does not use it today
- Rule: treat CLAUDE.md environment claims as hints, not facts — verify against
  the code (grep the env var name) before repeating them to the user

## 2026-07-05 — External volume Write can fail transiently
- Trigger: Write tool returned `EACCES: permission denied, mkdir '/Volumes/APPLE EXTERNAL SSD '` on a path that had just worked
- Wrong: n/a (first occurrence)
- Right: retrying the identical Write succeeded immediately
- Rule: on EACCES writing under "/Volumes/APPLE EXTERNAL SSD /", retry once before
  changing approach; if it persists, fall back to Bash heredoc with quoted paths

## 2026-07-05 — Write tool refuses to overwrite unread files; .claude/rules/ is auto-loaded
- Trigger: `Write CLAUDE.md` failed with "File has not been read yet" after a session restart
- Wrong: assumed having the file content in context (via system reminder) counts as having read it
- Right: the Write tool requires an actual Read call on an existing file in the same
  session before overwriting; a short `limit` Read is enough. Separately observed:
  this harness auto-loads every `.claude/rules/*.md` as project instructions each session.
- Rule: before overwriting any existing file, Read it first (limit 10 is fine).
  Treat `.claude/rules/*.md` as ALWAYS-LOADED context — keep them lean; put
  on-demand content in `.claude/refs/` instead

## 2026-07-05 — Parallel agents shipped a producer/consumer mismatch
- Trigger: provision-pgsql.mjs (agent 1) bundled only the 6 binaries the OLD code
  invoked; agent 2 concurrently added pg_restore usage, and pg_dump (backups) was
  missed too — both agents' self-verification passed; only the commander's final
  cross-diff review caught it (runtime would fail with BinaryNotFound)
- Wrong: each agent verified its own artifact in isolation; the bin list was
  derived from the existing tree instead of from the consuming code
- Right: derive the required-binaries list by grepping `pgsql_bin.join` callers in
  postgres.rs; after parallel delegations, the commander must explicitly check
  producer artifacts against consumer code (what is provided vs what is invoked)
- Rule: when two subagents build artifacts where one consumes the other's output,
  always run a final cross-check that greps the consumer for everything the
  producer must supply — self-verification of each half is not sufficient

## 2026-07-06 — Token-storage migration: hidden session-minting path + stale plan facts
- Trigger: P0 token-storage fix (localStorage → in-memory access token + HttpOnly
  refresh cookie). `grep AuthResponse` found src/services/passkey.rs:314 also builds
  a session (passkey login), not just password login in handlers/auth.rs.
- Wrong: (a) plan said CORS List branch needed `.allow_credentials(true)` added —
  it was ALREADY present at routes/mod.rs:112. (b) Scoping only the password login
  path would have left passkey login putting refresh_token in the JSON body.
- Right: any handler returning AuthResponse/RefreshTokenResponse mints a session and
  must set the cookie — grep the RESPONSE type, not just the endpoint named in the
  spec. `Secure` cookies are accepted by browsers over http://localhost (dev proxy
  works, no cookieDomainRewrite needed since the Vite proxy is same-origin).
- Rule: for any auth-transport change, grep for every construction site of the
  session-response struct (here AuthResponse) and treat each as an entry point;
  verify plan claims about existing code (CORS flags, config) against the file
  before repeating them — they may already be done.

## 2026-07-06 — SameSite=Strict cookie migration silently breaks the Tauri desktop build
- Trigger: independent second-opinion review of the P0 cookie migration (fresh
  general-purpose agent + commander read-through), required by
  judgment-rubrics.md rubric #1 for auth-touching changes.
- Wrong: the implementing (opus) agent verified the refresh-cookie flow with a
  curl cookie-jar sequence against the plain HTTP server and declared it done.
  That test can't catch cross-origin cookie behavior because curl doesn't
  enforce SameSite/origin semantics the way a browser/webview does.
- Right: `hotel-desktop`'s Tauri webview loads the frontend from
  `tauri://localhost` (macOS/Linux) / `https://tauri.localhost` (Windows), but
  the backend sidecar is `http://127.0.0.1:<dynamic port>` — a different origin,
  so every request is cross-site. `SameSite=Strict` (and even `Lax`) cookies are
  never sent on cross-site fetch/XHR, only `Lax` allows top-level GET
  navigation. Since `AuthContext.tsx` calls the refresh endpoint on every app
  mount, the desktop build will never restore a session after a full restart —
  it degrades gracefully to the login screen (not a crash), but it is a real UX
  change from the previous (insecure) localStorage-persisted-across-restarts
  behavior. User decision: accepted as-is (desktop re-login on every restart is
  a reasonable trade-off for removing the XSS-exfiltration risk); no
  desktop-specific persistence path was built.
- Rule: any cookie-based auth change must be checked against EVERY origin that
  consumes the API, not just the browser-facing production/dev origin — grep
  `tauri.conf.json` / equivalent embedding configs for the actual scheme+host
  the frontend loads from, and compare against the backend's bind
  address/port. A same-process curl test cannot substitute for this; state
  explicitly in the report which origins were and weren't exercised.

- Wrong: treating "compiles under --all-features + clippy clean" as sufficient for new runtime SQL strings; trusting column names from a mapping report instead of the DDL.

## 2026-07-10b — room_events / room_history: tables referenced by SQL that were never migrated at all
- Trigger: live 500 on PATCH /api/housekeeping/tasks/{id}, diagnosed from
  hotel-app-be/logs/backend-*.log: "current transaction is aborted, commands
  ignored until end of transaction block". Root query was `INSERT INTO
  room_events` inside a `let _ = sqlx::query(...)` in
  src/services/rooms.rs:216 — the error was swallowed, but Postgres had
  already poisoned the transaction, so the NEXT statement (audit_logs insert)
  failed instead and that unswallowed error is what surfaced as the 500.
  Grepping confirmed `room_events` had ZERO CREATE TABLE in schema.sql or
  src/repositories/rooms_queries.rs (INSERT/SELECT). Follow-up grep found the
  hit "no such table: room_history" on every check-in/check-out.
- Wrong: assuming a table exists because repository code queries it. Also:
  `let _ = sqlx::query(...)` inside a Postgres transaction is not a safe way
  to make a write "best-effort" — a failed statement aborts the whole
  transaction regardless of whether Rust looks at the Result, so the failure
  just resurfaces on the next statement with a confusing unrelated error.
  columns matched against the actual RoomEvent/RoomHistory struct field types
  in src/models/room.rs (e.g. scheduled_date is TIMESTAMPTZ/DateTime<Utc>,
  not DATE). Verified by: applying schema.sql to live Postgres, replaying all
  exact INSERT+SELECT shapes the Rust queries use, and re-running the exact
  failing multi-statement transaction directly in psql to confirm it no
  longer aborts. `cargo check` passed on both feature sets throughout —
  compile success never caught any of this.
- Rule: when a 500 traces to "transaction is aborted" or "relation/no such
  table by name before assuming it's a data problem — if the CREATE TABLE
  doesn't exist in one or both, that's the bug, not a stale-migration issue
  (contrast with the ota_reference case). Never trust `let _ =` around a
  `sqlx::query` inside a transaction as "safe to ignore" — in Postgres it
  isn't; either propagate the error or wrap the statement in a SAVEPOINT.


## 2026-07-10 — TypeScript 7 blocked by Bun: side-by-side TS6 bridge is not expressible
- Trigger: chore/ts7-upgrade branch. typescript@7.0.2 (native) passed typecheck (1.6s)/test/build/build:tauri, but lint hard-crashed — @typescript-eslint/typescript-estree needs the TS JS API, which TS 7.0 does not ship (returns in 7.1; typescript-eslint supports `>=4.8.4 <6.1.0`)
- Wrong: assumed Microsoft's recommended @typescript/typescript6 side-by-side bridge could be wired up under bun. Attempt 1: nested `"overrides"` → `warn: Bun currently does not support nested "overrides"`. Attempt 2: alias flip (`typescript` → npm:@typescript/typescript6, `typescript-native` → npm:typescript@~7.0) → bun re-resolved the bare name `typescript` inside the second alias through the FIRST alias, installing the compat package under both names and leaving `.bin/tsc` a dangling symlink
- Right: hold at typescript@~6.0 (all gates green; 6.0-clean code compiles identically under 7.0 by design). Adopt 7 when typescript-eslint ships TS 7.1 API support — no config tricks needed then
- Rule: bun does not support nested overrides and mis-resolves self-referential `npm:` aliases (both verified 2026-07-10) — do not attempt npm/pnpm override recipes under bun; after any aliased install, verify by reading the installed package's package.json `name`/`version`, not by install success. Also: TS 6.0 hard-errors on `alwaysStrict` in tsconfig (TS5107), and browser code must use `ReturnType<typeof setTimeout>` not `NodeJS.Timeout`

## 2026-07-12 — cargo check passes while test targets are broken; signature changes must run cargo test to verify
- Wrong: treating a clean cargo check (both feature sets) as proof a signature change is complete
- Right: cargo check does not compile tests/ targets; integration tests calling service functions directly break invisibly until cargo test (or cargo check --tests) runs
- Rule: after changing ANY pub fn signature in hotel-app-be, grep hotel-app-be/tests/ for the fn name AND run cargo test (or at minimum cargo check --tests) before claiming done — cargo check alone is insufficient even with all feature flags

## 2026-07-12 — booking/ledger refs described a dead two-layer architecture
- Trigger: 13-agent scan (independent verifier confirmed) found
  `.claude/refs/booking-workflow.md` and `ledger-workflow.md` still described
  business logic as living in `handlers/bookings.rs`/`handlers/ledgers.rs` at
  anchors like `create_booking_handler:537`. Re-grepping showed those handler
  files are now 264 and 129 lines of thin wrappers; the real logic moved to
  `repositories/bookings/lifecycle.rs` (4259 lines) and `repositories/ledger.rs`
  (2335 lines) at a prior refactor whose commit wasn't cross-checked against refs.
- Wrong: trusting refs written 2026-07-05 without re-verifying anchors before an
  agent used them to navigate — `create_booking_handler` was grepped at
  lifecycle.rs:1183, not bookings.rs:537 (off by >600 lines and a different file).
- Right: also found a three-layer nuance worth preserving — `services/bookings.rs`
  holds real logic (`void_booking:51`, `manual_checkin:153`, permission/ownership
  checks beyond route-level RBAC) while `services/ledgers.rs` is pure 1-line
  passthrough to `repositories::ledger`. The two domains diverged in whether the
  service layer does anything; a rewritten ref must say so explicitly rather than
  assuming symmetry between domains that look structurally similar.
- Rule: refs describing "where logic lives" rot silently when a refactor moves
  logic between layers without touching the ref (nothing forces that link). Any
  session doing a large repository-layer refactor should grep
  `.claude/refs/*.md` for the old file's name and flag stale refs in the same
  commit, not leave it for a future scan to discover. Rewrote both refs in
  this pass; every anchor re-verified via direct grep same-session, not
  reused from the scan report's claims.
  (Salvaged 2026-07-17 from the uncommitted unruffled-hellman worktree.)

## 2026-07-15 — seed.sql has a self-validating bootstrap transaction; adding a system permission/route/action touches ~6 lists, not 1
- Trigger: Phase 2 of the communications build added 5 `communications:*` permissions, a `/communications` nav route, and two new actions (`compose`,`send`). Appending them after seed.sql's `COMMIT` (the "obvious" idempotent spot) failed, then a cascade of DIFFERENT bootstrap-validation `RAISE EXCEPTION`s fired on each fix. Only a real `postgres:19beta1` docker apply (twice, for idempotency) surfaced them — `cargo check` compiles the SQL as an opaque `include_str!` and catches NONE of this.
- Wrong: (a) append-after-`COMMIT` — seed.sql wraps lines ~14–1425 in one `BEGIN…COMMIT` with an in-txn validation DO block (~1300–1420) that `RAISE`s if any `expected_route_access_policies` row lacks a matching `route_access_policies` row, so the route MUST be inserted inside the txn. (b) Assuming ONE action allowlist — `valid_action` is defined FIVE times: an inline CHECK in the permissions CREATE TABLE + three idempotent `ALTER…DROP/ADD CONSTRAINT` re-assertions in schema.sql, PLUS a copy in seed.sql, PLUS the quarantine/delete reconciliation (3 sub-copies) AND a final "invalid system-owned records remain" counter — a new action must be added to ALL of them or the DB re-apply aborts. (c) Not registering perms in `expected_system_permissions` (seed.sql ~line 45) — a `DELETE FROM permissions WHERE is_system_permission AND NOT EXISTS (in expected_system_permissions)` silently removes them before the route-permission-reference validation then reports them "unknown".

- Wrong: treating the schema append as done after live-apply verification; also the legacy-import test replays the WHOLE schema file then seeds a 1..=28 ledger, so any new section containing a non-idempotent statement (ALTER TABLE ADD COLUMN) fails on replay.

## 2026-07-17 — Workflow subagents at top-tier model died on the monthly spend limit; cost-tiering fixed it
- Trigger: guest-portal review workflow, 8 agents inheriting the main-loop model (fable) all failed with "You've hit your monthly spend limit" after burning ~713k tokens with zero results returned
- Wrong: launching a wide fan-out with every agent on the session's (top-tier) model; no early abort when the first agent failed
- Right: relaunch with explicit tiers — haiku for mechanical evidence-gathering reviewers, one opus adjudicator for judgment, sonnet for targeted re-checks — completed 10/10 agents (~1.8M tokens, mostly haiku/sonnet) and the opus adjudicator caught haiku hallucinations (fabricated evidence details) while confirming 20/21 findings; a 1-call haiku "canary" agent at the workflow start makes the spend-limit failure mode cost ~nothing
- Rule: in Workflow scripts, always set model explicitly per stage (haiku=gather, opus/sonnet=judge) instead of inheriting the session model, and open with a trivial haiku canary agent that aborts the run if it returns null


## 2026-07-19 — SameSite=Strict refresh cookie bounces admins out in Safari (works in Chrome); Salim Inn WebGL landing needs fixed-bar layer promotion
- Trigger: user reported "unable to click admin console" in desktop Safari on the localhost index page, then "redirect back after direct to the admin page and fail". The index page is the static WebGL landing hotel-web-fe/salim-inn/index.html (React `/` route LandingPage.tsx does window.location.replace('/salim-inn/index.html?account=...')); its "Admin console" pill is `<a href="/admin-portal" target="_top">`.
- Two distinct bugs, both Safari-only:
  1. CLICK: the #webgl `<canvas>` (position:absolute;inset:0 inside a sticky .stage) fills the viewport from y=0, spatially under the fixed 84px .topbar. Chromium hit-tests the fixed bar on top (verified document.elementFromPoint → A#accountAction), but Safari composites the GPU-backed canvas ABOVE a plain position:fixed element regardless of z-index:40, swallowing the clicks. Fix: promote the bar to its own layer — `.topbar{-webkit-transform:translateZ(0);transform:translateZ(0);will-change:transform}` + `.brand,.top-actions{position:relative;z-index:1}`. (Also added -webkit-backdrop-filter to the 5 glass panels — unprefixed backdrop-filter is Safari 18+ only; and `.pill[hidden]{display:none}` because `.pill{display:inline-flex}` was defeating the UA [hidden] rule so admin-mode never hid "Book stay".)
  2. SESSION: after the click navigates (full page load) to /admin-portal, AuthContext re-mints the access token by POSTing /api/auth/refresh (in-memory access token + HttpOnly refresh cookie design). The refresh cookie was `SameSite=Strict; Path=/api/auth` (hotel-app-be/src/handlers/auth.rs build_refresh_cookie). Backend refresh roundtrip works via curl (200) and the same-origin fetch returns 200 in Chromium — but Safari does NOT reliably attach a Strict cookie to the fetch that fires immediately after a top-level link navigation, so refresh 401s, isAuthenticated stays false, and RootLayout.tsx:60 bounces the admin to /login (perceived as "redirect back"). Fix: SameSite::Strict → SameSite::Lax on build_refresh_cookie AND clear_refresh_cookie. Lax is sent on all same-site requests in every browser incl. Safari, still withheld from cross-site POSTs so CSRF protection on the POST refresh endpoint holds. In-memory token is lost on every full page load by design, so the landing→/admin-portal hop always depends on the cookie.
- Rule: a `SameSite=Strict` cookie that a SPA must send on the FIRST request after a top-level navigation (session-restore-on-load, refresh-token cookies) is a Safari footgun — it works in Chrome and in curl but Safari drops it, so default to `SameSite=Lax` for refresh/session cookies (Lax still blocks cross-site POST CSRF). Existing users keep the old Strict cookie until they re-login, so a cookie-attribute change requires re-login to take effect. Separately: any fixed/sticky UI over a WebGL/`<canvas>` or 3D-transformed layer must be GPU-promoted (translateZ(0)/will-change) or Safari composites the canvas on top and eats the clicks — Chromium's elementFromPoint will NOT reveal this (it hit-tests correctly); only Safari misbehaves.

## 2026-07-19b — CORRECTION: the admin-portal bounce was a half-up docker stack on :80, not SameSite
- Trigger: after the SameSite=Lax fix + re-login, user still bounced. Asking them to type localhost:3000/admin-portal was never tested — their Safari showed bare "localhost" (no port) = port 80, which `lsof` revealed is the `hotel-frontend` DOCKER container (OrbStack), while all my verification ran against the Vite dev stack on :3000.
- Wrong: attributing the bounce to Safari SameSite=Strict fetch behavior (plausible, but never reproduced — Strict refresh returned 200 in Chromium and was never isolated in Safari). Also wrong: assuming "localhost" in the user's browser meant the dev server.
- Right: the compose topology (docker-compose.yml:71-97) is Caddy (`--profile https`) → `/api` to backend, rest to frontend; the frontend nginx has NO /api route by design (verified: POST /api/auth/login on :80 → 405 text/html from the SPA fallback). Locally only the frontend container was up (backend Exited 11h, no caddy), so http://localhost could never authenticate — every full-page load's cookie refresh fails and RootLayout bounces off /admin-portal in ANY browser. The frontend image (built 2026-07-18T07:42Z, no volume mounts) also predates all of today's CSS fixes. Keep the SameSite=Lax change anyway (standard for refresh cookies; Strict on a session-restore-on-load cookie remains a documented WebKit risk — tag: unverified here).
- Rule: when a web bug "only happens in browser X", FIRST establish which server that browser is actually hitting — bare "localhost" (no port) means :80, and on this machine :80 is the docker frontend, not Vite. `lsof -iTCP:80 -sTCP:LISTEN` + `docker ps -a` before any client-side theory. A stale/half-up docker stack on :80 shadows the dev stack and produces "works in my test browser, fails in the user's" purely by port mismatch.

## 2026-07-24 — the V1 PG baseline could not install at all; hand-edits into a pg_dump file must respect its three-phase ordering
- Trigger: verifying repo baseline against the live VPS (`saliminn-db`). A fresh apply of `database/postgres/migrations/0001_v1_baseline.sql` to a scratch `postgres:19beta2` container failed at statement 1: `ERROR: there is no unique constraint matching given keys for referenced table "payments"`. Commit 9b92a8eec had appended `CREATE TABLE public.payment_receipt_requests (payment_id bigint PRIMARY KEY REFERENCES public.payments(id) …)` inline right after `CREATE TABLE public.payments`, but the file is pg_dump-shaped: table bodies first, then `ALTER TABLE … ADD CONSTRAINT … PRIMARY KEY` (payments_pkey at ~line 5764), then FKs (~line 9032). The FK referenced a PK that did not exist yet. Same commit's `seed.sql` additions then failed with `promotions_updated_by_fkey … Key (updated_by)=(1) is not present in table "users"` — the seeded admin is id **1000** (seed.sql:50), not 1.
- Wrong: treating a `.sql` baseline as append-anywhere text, and hardcoding `created_by, updated_by = 1, 1` in seed rows. Neither is caught by `cargo check --all-features`, clippy, `cargo test --test status_vocabulary` (all passed on the broken file — the baseline is an opaque `include_str!`), or `scripts/check-schema-drift.mjs` (exits 0; it is a report of known engine differences).
- Right: new tables in the PG baseline go in three places — CREATE TABLE (no inline PK/FK), `ADD CONSTRAINT …_pkey` in the constraints block, `ADD CONSTRAINT …_fkey` in the FK block. Seed rows needing an audit user must resolve it (`SELECT id FROM users WHERE username = 'admin'`), never a literal id. Also: an existing-DB patch should be written in the baseline's own expression spelling (`(status)::text = ANY (ARRAY[('x'::character varying)::text, …])`) rather than `status IN (…)` — both are semantically identical but pg_dump renders them differently, and the difference shows up forever as fake drift.
- Rule: after ANY edit to `database/postgres/migrations/0001_v1_baseline.sql` or `seed.sql`, apply both in order to a scratch `postgres:19beta2` container (`docker run` + `psql -v ON_ERROR_STOP=1`) before claiming done — Rust compilation and the test suite cannot detect a baseline that will not install. Remember to mirror the result into `hotel-desktop/src-tauri/database/postgres/` (it holds byte-identical copies).

## 2026-07-25 — salim-inn WebGL scene had a plan/height scale split; instanced props need matrix-level debugging
- Trigger: rebuilding hotel-web-fe/salim-inn from Google Earth footage. Buildings rendered as ~25-storey towers on a floating map.
- Wrong: the scene divided plan coordinates by `SCALE=10` (map px → units) but left heights in raw metres (`BUILDING_H=8.4`, `FLOOR_H=2.8`). Plan and height were on scales ~8x apart, so a 3-storey shophouse extruded to ~84 m. `SALIM.angle` was also used for orientation, which points the frontage 180° from `SALIM.frontNormal` — invisible while the ground was a flat map screenshot, wrong as soon as there is a modelled road to face.
- Right: 1 map px = 1 m (verified two ways: the dual carriageway measures ~22 px against a real ~22 m; the Salim row measures 71.6 x 29.4 px against a twelve-lot shophouse block). Orient from `frontNormal` via `Math.atan2(-nx, -nz)`, never from the raw rectangle angle.
- Rule: when debugging "what is that object in front of the camera", a `scene.traverse` that reads `getWorldPosition()` SILENTLY SKIPS every InstancedMesh instance — an InstancedMesh reports its group origin, so a tent 8 m from the camera looks 70 m away. Decompose per-instance matrices (`getMatrixAt(i, m)` + `setFromMatrixPosition`) or the scan is worthless. Two related gotchas in the same pass: `PlaneGeometry` faces +Z, so canvas-texture signage on a facade that faces local -Z needs `rotation.y = Math.PI` or it renders backwards into the wall; and after rotating a props group, local -Z may point at the opposite building from the one intended — check which world direction it resolves to before placing anything.

## 2026-07-25b — tracing a site plan: use the capture's own scale bar, and pin left/right from the frontage normal
- Trigger: user supplied a Google Earth top-down of the Farley block after the first rebuild, saying cafe.cafe was on the wrong side of Salim Inn, the neighbour should lean inwards, and the buildings were misplaced. All three were correct.
- Wrong: the first trace came from a Google *road map* screenshot at an unknown zoom. Blocks ended up in roughly the right relationship to each other but the wrong place relative to the two buildings that matter, and the row was modelled as one straight block when it actually bends twice.
- Right: the Google Earth capture carries a scale bar — measure it in pixels rather than estimating. Detected the rule row by scanning for a run of bright pixels (`ffmpeg -vf crop,format=gray -f rawvideo` then find the row with the widest bright span): 320.5 px for 40 m, so 0.12481 m/px. Then `drawgrid=w=200:h=200` over the capture gives a readable coordinate frame to read POI pins off directly. Frontage resolved to a 3-segment polyline, 122.8 m total, bending 26° then 43° inward.
- Rule: "left" and "right" in a user's description of a building are from the FRONTAGE view, not from image or world axes. Derive them: for frontage direction d and outward normal n = (d.z, -d.x), a viewer standing outside facing the building has their right along -d, i.e. toward DECREASING distance along the polyline. Verify it numerically by projecting both landmarks into camera NDC and comparing x — do not eyeball it from a render. Also: when a terrace bends, sweep it per-lot along the polyline with `ry = atan2(-dz, dx)` per lot; one long rotated box cannot express a dog-leg, and the interior fitted inside it has to inherit the local bearing too.

## 2026-07-26 — full-system eval: pipe-masked gate exits, a stillborn SQL-function endpoint, --all-targets-only clippy errors
- Trigger: 7-dimension evaluation sweep (32-agent workflow) + CI gate re-runs. Three independent discoveries.
- Wrong: (1) background gates written as `cargo clippy … | tail -30` and `a && b && c | tail` reported exit 0 while the gates actually FAILED — a pipeline's exit code is the LAST command's (tail's). Also `bun --cwd <dir> run <script>` silently prints usage and runs nothing; the flag belongs after `run`. (2) Assuming a routed, RBAC-gated endpoint implies its SQL objects exist: `POST /rooms/sync-statuses` called `SELECT * FROM sync_all_room_statuses()`, a function with ZERO definitions anywhere in git history (`git log -S sync_all_room_statuses --all -- "*.sql"` is empty) — the endpoint 500'd since birth, no FE caller ever existed. (3) Believing CI-green means clippy-clean: `items_after_test_module` (items appended below `mod tests` in credits.rs/audit.rs) only fires with `--all-targets`, which CI does not pass.
- Right: removed the dead endpoint chain (route/handler/service) instead of inventing sync semantics — judgment-rubrics #3 forbids guessing business policy; a task chip offers the real implementation as a user decision. Gates re-run bare (no trailing pipe) with real exit codes: clippy --all-features --all-targets clean, lib tests pass, FE typecheck/lint/260 tests pass.
- Rule: never end a verification command in a pipe — run it bare or append explicit `echo EXIT=$?` markers; extend the room_events missing-table rule to FUNCTIONS: every `SELECT … FROM some_fn()` in runtime Rust must have a matching CREATE FUNCTION in the baseline (grep it, and `git log -S <name>` to check it ever existed); after appending items to a Rust file, confirm they land above `mod tests` or clippy --all-targets breaks while CI stays green.
- Status update (2026-07-26, later session): the user chose to implement the real feature. `sync_all_room_statuses(p_user_id)` now exists in the PG baseline (mirrors the sync_room_status_with_booking trigger policy; never overrides dirty/cleaning/reserved_dirty/maintenance/out_of_order), the route/handler/service chain is reinstated with an audit_logs entry, and a "Sync statuses" button was added to HousekeepingPage (gated on rooms:update). Verified by scratch postgres:19beta2 install + live curl (found 15 rooms of genuine drift in the dev DB). The endpoint is no longer dead — do not re-remove it.

## 2026-07-26c — PG19 physical-design rewrite: dump-diff convergence is the only check that catches everything
- Trigger: full V1 baseline rewrite (68 serial→identity via SEQUENCE NAME, 4 STORED→virtual generated columns relocated to end-of-table, 7 ledger columns→timestamptz).
- Wrong: (a) the coupling-audit subagent reported 3 explicit-id test INSERTs; a script sweep at edit time found 16 (users/room_types/rooms/guests/bookings across booking_service.rs fixtures) — grep-audit reports rot instantly, re-sweep by script when editing. (b) The patch's drop/re-add of customer_ledgers.balance_due silently lost its COMMENT ON COLUMN — nothing compiled, no test failed; ONLY the fresh-vs-patched pg_dump diff caught it.
- Right (all verified empirically on postgres:19beta2): pg_dump renders VIRTUAL generated columns with NO trailing keyword (bare `GENERATED ALWAYS AS (expr),` = virtual; STORED is always explicit). `OVERRIDING SYSTEM VALUE` is accepted on tables with no identity column, so a generic INSERT builder (data_transfer) can add it unconditionally. pg_get_serial_sequence()/setval()/`SELECT last_value FROM seq` keep working on identity sequences when SEQUENCE NAME preserves the serial-era name. pg_dump 18+ emits random `\restrict`/`\unrestrict` tokens — strip them before diffing dumps. STORED→VIRTUAL has no in-place ALTER; drop+re-add moves the column to the last attnum, so the baseline must also declare those columns last or fresh/patched schemas diverge forever.
- Rule: any baseline rewrite ships with (1) an idempotent patch for existing V1 DBs and (2) the convergence proof: scratch-install NEW trio, scratch-install OLD trio + patch, `pg_dump --schema-only --no-owner --no-privileges` both, strip `\restrict` lines, diff MUST be empty. Compile, clippy, and the full test suite all passed on a patch that was still losing a column comment — only the dump diff saw it.

## 2026-07-26d — adversarial opus review caught what every green gate missed: panicking Row::get decodes and a silent-money deployment gap
- Trigger: post-implementation review (fresh opus agent) of the pg19 rewrite returned FAIL with 3 blockers after check/clippy/full tests/live API probes were ALL green.
- Wrong: (a) the tier-3 coupling audit scoped models/+repositories/ledger.rs and missed repositories/analytics.rs:2354,2415 where `Row::get::<NaiveDateTime>` (get, not try_get → unwrap → panic) reads customer_ledgers/customer_ledger_payments.created_at — zero tests cover those report paths, so the suite proved nothing there. (b) Rust `.and_utc()` on a parsed naive noon stored 12:00Z while the patch's `AT TIME ZONE` conversion stored 04:00Z for the same logical date — two instants for one business date. (c) On an UNPATCHED database the row_mappers `.ok()`/default fallbacks turn the decode failure into epoch dates and a un-zeroed voided-ledger balance — silent wrong money, not an error.
- Right: decode timestamptz as DateTime<Utc> everywhere sqlx touches it (grep the TABLE names across ALL of src/, not the module you think owns them); make date→instant conversion happen in SQL (`$n + INTERVAL '12 hours'` on a date param) so the session (hotel) timezone interprets it identically to the patch; add a startup schema guard (information_schema data_type probe → process::exit) so a schema-generation mismatch is loud everywhere (VPS, desktop, dev) instead of silently degrading.
- Rule: any change that retypes columns decoded by sqlx MUST (1) grep the whole backend for the table names (not the domain module) hunting `NaiveDateTime`/`Row::get`, and (2) ship a startup guard that refuses schema generations older than the code — mapper fallbacks make type mismatches SILENT, so "the app boots and tests pass" proves nothing about unpatched deployments. Budget an adversarial fresh-context review for every money/schema change; this one paid for itself.

## 2026-07-26e — pg reactivation test flaked on rerun: trigger-written room_status_change_log FK-pins fixture rooms
- Trigger: postgres_concurrent_reactivation_allows_only_one_success passed on a fresh DB, panicked on rerun (duplicate key room_types_code_key) — reproduced on scratch postgres:19beta2
- Wrong: cleanup_pg_fixture deleted rooms/room_types with `.ok()` but never room_status_change_log; reactivation's UPDATE bookings fires trg_sync_room_status_booking → update_room_status() → INSERT INTO room_status_change_log (FK room_id, NO cascade), so DELETE FROM rooms silently failed every run and the leftovers collided with the reseed. The sibling postgres_creation_tests cleanup already deleted that table — divergence between sibling fixtures was the tell.
- Right: cleanup deletes room_status_change_log by room_id before rooms (room_history cascades from ROOMS, so it needs no delete when deleting a room — but see the 2026-07-27 correction below: `room_history.changed_by` → `users` does NOT cascade, so deleting a test ACTOR is FK-pinned by it), AND fixed-id seeds are ON CONFLICT DO UPDATE resets so the pre-test state ('voided' booking, 'available' room) is guaranteed even when a delete is FK-blocked. Verified: fresh-DB double run, polluted-DB double run, and a pinned-rows double run (payments + housekeeping_tasks rows deliberately blocking the deletes) all pass.
- Rule: any PG test that mutates bookings.status or rooms.status writes room_status_change_log via trigger — its cleanup must delete those rows explicitly; and fixed-id fixtures against a persistent DB must upsert-reset state, never plain-INSERT behind a best-effort `.ok()` delete.

## 2026-07-26f — FE hotel-timezone date rendering: bare localStorage is unusable in vitest; helpers must handle BOTH schema generations
- Trigger: FE fix for the ledger timestamptz change (business dates now render in the hotel timezone via new utils/date.ts helpers reading hotelSettings 'timezone', fallback Asia/Kuala_Lumpur). New date.test.ts failed 8/8 with `localStorage` undefined DESPITE vitest.config.ts `environment: 'jsdom'` (Node 26's global localStorage getter yields undefined; jsdom env does not fix the bare global here).
- Wrong: assuming environment:'jsdom' provides a working bare `localStorage` global in this repo's vitest setup; also nearly treated "Z-suffixed instants" as the only wire shape while the LOCAL dev hotel-db was still `timestamp without time zone` (pre-patch — the 2026-07-26 patch had not been applied to it as of this session, so the new backend's schema guard blocks full-stack dev verification until it is).
- Right: every repo test touching localStorage stubs it (`vi.stubGlobal('localStorage', …)` or Object.defineProperty — see currency/notificationStore/dashboardUtils tests); production code paths that read it must guard (getHotelSettings' try/catch makes utils/date.ts safe in non-DOM runtimes). The date helpers branch on wire shape: date-only and zone-less naive strings pass through literally (pre-patch backends keep exact old behavior), only zone-carrying strings/Date instants get Intl timeZone conversion — verified with real dev-DB rows under TZ=America/New_York, UTC, and Asia/Kuala_Lumpur (KL output unchanged).
- Rule: in hotel-web-fe tests, never touch bare `localStorage` — stub it via vi.stubGlobal like the existing tests; and any FE formatter for backend timestamps must detect zone info per value (date-only / naive / zoned) rather than assume one shape, because patched and unpatched deployments coexist during a schema-generation rollout.

## 2026-07-26g — Edit "success" on this volume can silently revert; only an independent reviewer caught it
- Trigger: tauri.conf.json resources edit reported success, cargo/clippy passed, then the SSD flaked ("Working directory was deleted" mid-session); an adversarial opus review of the diff found the file back at its pre-edit content (git clean, no patches entry) while sibling edits made in the same window survived
- Wrong: trusting the Edit tool's success result as proof of persistence on "/Volumes/APPLE EXTERNAL SSD /"; also my re-check grep used a paraphrase of the written text (missed a backtick) and false-alarmed a second file as lost
- Right: after any volume flake event (EACCES, cwd-deleted), re-verify every recent Write/Edit by grepping for an EXACT substring of the new content, and confirm via `git status` that the file shows as modified
- Rule: on this volume, treat a cwd-deleted/EACCES flake as a checkpoint — grep-verify all edits from the preceding minutes before building on them; and never skip the independent review for schema/packaging changes, it is what caught the silent revert

## 2026-07-26h — desktop pgsql provisioning fail-soft shipped a wrong-version tree for 9 days; version truth lives in postgres.rs constants
- Trigger: build-setup audit found the bundled PostgreSQL runtime did not match the major version required by `postgres.rs`; `ensure_postgres_running` correctly failed, but the preparation script had treated provisioning failure as a warning and CI checked only compilation.
- Wrong: warn-and-continue on provisioning failure whenever a pgsql/ tree exists, regardless of whether that tree matches the required build; assuming brew can supply the pin (Homebrew core has no postgresql@19 formula — betas are not shipped)
- Right: provision-pgsql.mjs now exits 2 when the existing tree is CONFIRMED wrong (version/build mismatch, inconsistent or missing binaries, mismatched manifest) vs 1 when merely unverifiable; desktop-prepare hard-fails on 2. Getting 19beta2 on macOS requires a source build (or POSTGRES_PREFIX to one) — brew cannot provide it
- Rule: a bundled-runtime version requirement in code (CONFIGURED_POSTGRES_* in postgres.rs) must be enforced by the packaging path, not just at app runtime; any "keep going with the existing artifact" fallback needs to distinguish unverifiable-but-plausible from confirmed-wrong — after changing a required bundled version, re-run `bun run provision:pgsql` and check the manifest before trusting any build

## 2026-07-26i — Tauri CSP silently kills inline <script> blocks that survive the Vite build
- Trigger: audit adjudicator verified tauri.conf.json csp `script-src 'self'` (no inline, no nonce for srcless scripts — tauri-utils 2.8.1 html.rs only nonces script[src^='http']) against dist/: the salim-inn account-pill inline block and the root index.html guest-branding inline block both ship INLINE through vite build, so in the packaged webview neither runs — a signed-in desktop admin never sees the "Admin console" pill on the landing page (LandingPage.tsx redirects / there unconditionally)
- Wrong: assuming a script that works in browser dev/prod works in the desktop webview; browser serving has no CSP header here, so nothing ever flagged it
- Right: externalized both blocks (salim-inn/account-actions.js as a module entry-adjacent script; public/guest-branding.js as a classic head script keeping pre-paint timing). Also noted: csp img-src 'self' blocks the landing page's remote room photos in the packaged app (left for a vendoring decision)
- Rule: any inline <script> (or style beyond 'unsafe-inline') in HTML that the desktop webview loads is dead code under the packaged CSP — externalize scripts to files; when adding markup with scripts, grep tauri.conf.json csp and test against the PACKAGED app, not the dev server

## 2026-07-26j — desktop DB silently missed ALL seven dated patches; the auto-patch flow covers only pg19
- Trigger: first Windows build/run of the new sidecar. Backend started fine (pg19 auto-patch worked, pre-patch backup taken) but logged `relation "payment_receipt_requests" does not exist`; probing found ALL 7 patches dated 2026-07-21..24 missing — the desktop DB had received nothing since the July 17 PG19 upgrade, and sync_all_room_statuses() (baseline-only, no patch existed) was missing too
- Wrong: assuming a desktop DB that starts cleanly is schema-current.
- Right: validate schema-critical objects before launching the sidecar.
- Rule: legacy schemas are unsupported; export required data and rebuild from the current baseline and seed rather than mutating them in place.

## 2026-07-26k — Windows build-from-clean gotchas: unquoted npm_execpath with a space, stale FE deps, no bun
- Trigger: first `bun run build:no-bundle` on the Windows machine (user dir "C:\Users\SALIM INN" has a space) failed twice
- Wrong: build-frontend.mjs spawned npm_execpath with shell:true unquoted → cmd.exe ran `C:\Users\SALIM`; hotel-web-fe/node_modules predated @paypal/react-paypal-js; bun was not installed at all (npm i -g bun works)
- Right: quote the exec path when spawning through the Windows shell (fixed in build-frontend.mjs); run `bun install` in hotel-web-fe after pulling dependency changes before any desktop build
- Rule: on Windows, any spawnSync(cmd, args, {shell:true}) with a path argument must quote the path — a user directory with a space breaks it; and treat "Rolldown failed to resolve import X" as a stale node_modules signal, not a config problem

## 2026-07-26l — CORRECTION to 2026-07-26g: the "silent edit reverts" coincided with a concurrent session
- Trigger: after the desktop-build session's edits kept reverting (tauri.conf.json, hotel-web-fe/index.html), commit 886554d09 (21:21) appeared mid-session from a CONCURRENT session/user working in the same tree (it absorbed nearly all of this session's changes; separate eKYC/payment-approvals files were being modified simultaneously)
- Wrong: 2026-07-26g attributed the reverts solely to volume write-loss; with a second session active, `git checkout`/`git restore`-style operations explain files reverting to exact HEAD content at least as well — root cause is UNVERIFIED, not established
- Right: both files reverted to byte-exact HEAD state; the sibling edits made minutes apart survived — selective git restoration fits that pattern; storage write-loss fits it less well
- Rule: two agents/sessions must not share this working tree — use worktrees for concurrent work; and when files revert mid-session, FIRST check `git log` for commits newer than session start and look for signs of a concurrent session before blaming storage. The 2026-07-26g rule (grep-verify edits after any anomaly; independent review catches silent state drift) stands regardless of cause

## 2026-07-26m — pgsql 19beta2 provisioned from source; compiled datadir/libdir must carry the @major suffix
- Trigger: provisioning src-tauri/pgsql/ to 19beta2 (Homebrew has no postgresql@19; source build + POSTGRES_PREFIX + provision:pgsql:force per lesson 2026-07-26h).
- Wrong: a default-layout source build (`--prefix` only) ships a broken bundle because PostgreSQL binaries can resolve share/lib relative to the executable even when those paths do not exist in the packaged tree.
- Right: configure with --datadir=$PREFIX/share/postgresql@19 --libdir=$PREFIX/lib/postgresql@19 (the "postgres" substring stops Makefile.global appending another /postgresql; pkglibdir=libdir), ICU_CFLAGS/ICU_LIBS to skip pkg-config, `make world-bin` + `install-world-bin` (contrib is REQUIRED: baseline creates btree_gist/pg_trgm/pg_stat_statements), then mv the @19 dirs to unsuffixed names with @19 symlinks back so the provision script finds share/postgresql + lib/postgresql. Self-location verified by hiding ~/pg19beta2/share during a bundled-tree initdb/start/CREATE EXTENSION x3 smoke test (TCP-only: scratchpad paths exceed the 103-byte unix-socket limit — use -c unix_socket_directories='').
- Rule: bundled PostgreSQL tools must not retain absolute build-machine library paths. Rebuild from the verified source recipe and provision the bundle again when relocatability checks fail.

## 2026-07-26n — pgsql bundle made self-contained: relink step in provision-pgsql.mjs closes the relocation gap
- Trigger: follow-up to 2026-07-26m — bundled binaries referenced ~/pg19beta2/lib/postgresql@19/libpq.5.dylib and /opt/homebrew openssl@3/icu4c@78/readline dylibs by absolute path, and cpSync had copied the keg's unversioned libpq.dylib-style symlinks as absolute links into the source prefix, so any installer was DOA off this machine.
- Wrong: treating the copy step as the whole provisioning job; also assuming DYLD_PRINT_LIBRARIES reaches the postmaster through pg_ctl — it does not (pg_ctl spawns postgres via /bin/sh, a platform binary, and SIP strips DYLD_* at that exec), so trace the server by running bin/postgres directly.
- Right: provision-pgsql.mjs now has a post-copy relinkBundledTree() + assertRelocatable() step: copies every external dylib into pgsql/lib (libssl.3, libcrypto.3, libicui18n.78, libicuuc.78, libicudata.78 via @loader_path-companion chasing, libreadline.8), rewrites all refs to @loader_path-relative, rewrites absolute-external LC_ID_DYLIBs to @rpath/<name>, re-points absolute symlinks to in-tree siblings, codesign -f -s - each touched file (arm64 mandatory), then asserts zero external refs/absolute symlinks tree-wide. checkExistingInstall also gained a cheap guard (otool on the 7 bins + symlink walk) so an unrelinked tree is hard=true → exit 2, never shipped. Verified: ~/pg19beta2 renamed away, full initdb/start(shared_preload_libraries=pg_stat_statements)/CREATE EXTENSION x3/pg_trgm query/pg_dump -Fc/pg_restore -l cycle green; DYLD_PRINT_LIBRARIES on postgres/initdb/psql shows every non-system load coming from src-tauri/pgsql/lib; codesign -v clean on all Mach-O files. Tree grew 24MB→68MB (libicudata ~35MB — unavoidable while postgres links icu4c).
- Rule: 2026-07-26m's "not relocatable" caveat is CLOSED — do not re-flag it; ~/pg19beta2 is still needed to RE-provision (it is the copy source) but bundled binaries no longer reference it at runtime. After any re-provision, the script self-verifies, but the full end-user proof is: rename ~/pg19beta2, run the smoke cycle from the bundle, rename back. Remaining pre-existing distribution concerns (Gatekeeper/quarantine, tauri-side signing of resources) are unchanged by this work.

## 2026-07-26n — bun update <name> on a transitive dep silently ADDS it as a direct dependency
- Trigger: fixing bun-audit findings in hotel-web-fe; `bun update dompurify fast-uri brace-expansion undici` (bun 1.3.14) added undici@8.9.0 and brace-expansion@5.0.8 to package.json dependencies (major versions outside every parent's range) and left the two already-direct names untouched
- Wrong: assuming `bun update <pkg>` scopes to refreshing existing lock entries in-range like npm/yarn; mixing direct and transitive names in one invocation
- Right: `bun update <pkg>` only truly updates DIRECT deps; for a transitive, delete its line(s) from bun.lock (text format, one line per entry; nested keys like "glob/minimatch/brace-expansion" count) and run `bun install` — re-resolution picks the highest version satisfying the parents' ranges. `bun why <pkg>` shows direct-vs-transitive before choosing
- Rule: never pass a non-direct dependency name to `bun update`; fix transitives by removing their bun.lock entries + reinstall, then check `git diff package.json` for silently added dependencies before moving on

## 2026-07-26o — sqlx decode-type mismatch (NaiveDateTime vs DATE) shipped for 4 days; the reported site was not the only one
- Trigger: `GeneratedInvoiceBookingDetailsRow.check_in/check_out` (src/repositories/payment.rs) typed `chrono::NaiveDateTime` for `bookings.check_in_date/check_out_date` (DATE columns) — every `generate_invoice`/`get_invoice_preview` call failed at runtime with "NaiveDateTime ... is not compatible with SQL type DATE" since commit feea5d9558 (2026-07-22). Compile/clippy green throughout; zero test coverage on the path.
- Wrong: fixing only the reported struct. Grepping the COLUMN names across src/ found a second identical mismatch: `PaymentBookingStay` (src/models/payment.rs) decoded by `payment_booking_stay` → `calculate_payment_summary` → `create_payment` — payment recording was equally broken.
- Right: retyped both to `chrono::NaiveDate`, dropped the now-redundant `.date()` calls, and added tests/invoice_numbering.rs test (5) calling `services::payments::generate_invoice` + `calculate_payment_summary` end-to-end against live PG (5/5 pass; clippy --all-features --all-targets clean).
- Rule: when fixing any sqlx decode-type mismatch, grep src/ for the COLUMN names (not the struct name) and audit every struct/tuple that decodes them — mismatches travel in packs from the same misremembered schema; and any new FromRow struct over date/timestamp columns needs one live-PG test that actually fetches it, because sqlx type checking is runtime-only.

## 2026-07-26p — tracker sweep: ongoing-dev items rot like refs; placeholder/bind-count mismatches survive every gate
- Trigger: full ongoing-dev.md completion sweep (13-agent verify + 10-agent implement + 2 adversarial reviews). 11 of ~25 tracker items were ALREADY done (several by concurrent sessions the same day); the P0 claim "13 payments handlers login-only, no check_permission" was false against the tree — all 18 route wrappers call require_permission_helper (routes/payments.rs; merged at routes/mod.rs:218).
- Wrong: treating tracker lines as facts to implement (an agent adding check_permission to "13 handlers" would have produced duplicate guards); trusting that verbatim-moved SQL is safe because the move compiled and 200+ tests passed — the room_history INSERT in rooms_queries.rs declared 6 columns / VALUES($1..$6) with only 5 .bind()s, a runtime-only failure that aborts the whole room-change transaction.
- Right: verify every tracker item against the working tree BEFORE implementing (same discipline as refs line anchors); the sweep's adversarial opus reviewer caught the bind mismatch by re-deriving from the artifact, not the implementer's report. Fixed as VALUES(..., false) matching the sibling INSERT_ROOM_HISTORY convention; also wrapped the check-in occupancy UPDATE in a SAVEPOINT (2026-07-10b class) so a best-effort failure can no longer poison the tx and silently roll back a recorded payment.
- Rule: before implementing any tracker/backlog line, grep-verify its factual claim and downgrade stale items to doc edits; after moving or writing raw SQL with VALUES, count placeholders vs .bind() calls — check/clippy/tests cannot catch a mismatch, only runtime or review can.

## 2026-07-26o — zsh failed glob aborts the command; the echoed $? still read 0
- Trigger: sharp-usage verification grep `grep ... *.mjs *.json` printed `no matches found: *.mjs` — zsh aborted the entire grep without running it, and the `echo EXIT=$?` that followed showed 0 (taken from the aborted pipeline's tail), which looked like "grep ran, found nothing"
- Wrong: treating an echoed exit marker as proof the verification command executed; passing bare globs to grep in zsh
- Right: re-ran with `grep -r --include="*.mjs" --include="*.js" <dir>` (no shell globs) and got a real result; the earlier "no hits" was never evidence
- Rule: in verification commands never pass bare `*.ext` globs — use `grep -r --include=` or Glob/Grep tools; and if any command's output contains "no matches found", the command DID NOT RUN — its exit marker is meaningless, re-run before trusting any conclusion built on it

## 2026-07-26p — 2FA setup AND enable are both broken end-to-end; writing rbac_profile.rs tests surfaced two independent bugs
- Trigger: writing tests/rbac_profile.rs (RBAC/profile/2FA integration coverage). A live-PostgreSQL test calling `two_factor_service::enable_2fa` failed with `column "two_factor_recovery_codes" is of type text[] but expression is of type text`. Separately, grepping for `two_factor_challenges` (the table `setup_2fa` inserts into) found ZERO `CREATE TABLE` anywhere under `database/postgres/` — only the one INSERT site (src/core/auth.rs:741) references it.
- Wrong: assuming a routed, RBAC-gated handler with real business logic (setup/enable 2FA) must have a working write path just because `cargo check --all-features` and the full test suite were green — same class of gap as the 2026-07-10b missing-table and 2026-07-26o (invoice) sqlx decode-mismatch lessons, now hit twice in one file: (1) `setup_2fa` (src/services/two_factor.rs:39, via `AuthService::create_2fa_challenge`) always 500s with "relation two_factor_challenges does not exist" — the table was never migrated. (2) `AuthService::enable_2fa` (src/core/auth.rs:760-786) and `AuthService::update_recovery_codes` (src/core/auth.rs:808-830) both bind `core::db::array_to_json(&codes)` — a JSON `String` — against `users.two_factor_recovery_codes`, a genuine `text[]` column; every call fails at the database. `update_recovery_codes` is also used by `regenerate_backup_codes` and by `disable_2fa`'s recovery-code-consumption branch, so those are broken too. Only `AuthService::disable_2fa`'s plain NULL-clearing UPDATE and the TOTP-code branch of `disable_2fa` are unaffected.
- Right: did NOT fix src/ (out of scope for a test-only task); wrote the reachable subset live instead — seeded the "2FA enabled" end state directly with a native `Vec<String>` bind (not `array_to_json`) to sidestep the bug, then exercised `get_2fa_status`, `verify_2fa_code`, and `disable_2fa`'s TOTP branch for real against a live-generated TOTP code (`totp-rs` is already a normal dependency, so no new dependency was needed to avoid faking verification). Flagged both bugs via `spawn_task` instead of silently working around them.
- Rule: `core::db::array_to_json`/`json_to_array` are a SQLite-era shim; grep every call site against the actual column type before trusting a JSON-string bind targets a Postgres `text[]` — `enable_2fa`/`update_recovery_codes` prove `cargo test` green does NOT mean a write path works, because zero existing test exercised these functions before this session. When a test you're writing hits a real bug, do not silently skip it — seed around the specific broken call and keep testing everything reachable outside it; report the bug with exact file:line instead of just "skipped".
- Status update (2026-07-26, fix session): both bugs FIXED. `two_factor_challenges` was added to the baseline and desktop mirror; `enable_2fa`/`update_recovery_codes` now bind `Vec<String>` natively; the dead `array_to_json`/`json_to_array` shim was removed from core/db.rs. A third bug surfaced while writing the lifecycle test: `TwoFactorDisableRequest` validation capped `code` at 20 chars but recovery codes are 23 chars dashed, so the recovery-code disable path was unreachable through the service — max widened to 32. Verified with a fresh scratch install, `tests/rbac_profile.rs` Scenario 9, and clippy `--all-features --all-targets`.

## 2026-07-26q — desktop-build.yml proven green after three first-execution failures; paired npm/crate deps must not both float
- Trigger: proving desktop-build.yml by workflow_dispatch (ongoing-dev P1). Three iterations, each failing at the NEXT never-executed step: (1) icu4c keg-only (pre-fixed), (2) `library 'crypto' is required for OpenSSL` — openssl@3 is keg-only on macos-14 exactly like icu4c, (3) tauri CLI hard-error: crate tauri@2.11.1 (Cargo.lock) vs @tauri-apps/api@2.9.1 (bun.lock).
- Wrong: assuming one keg-only fix (icu4c) covered configure's needs — EVERY keg-only dep (openssl@3, lz4) needs explicit PKG_CONFIG_PATH/CPPFLAGS/LDFLAGS; and letting both halves of a paired ecosystem float (`tauri = "2"` + `"@tauri-apps/api": "^2"`) — Cargo.lock and bun.lock update on different days, so major/minor drift is guaranteed and the tauri CLI refuses to build on it.
- Right: run 30207504358 green end-to-end in ~12 min (PG 19beta2 source build with @19 datadir/libdir layout per 2026-07-26m, provision + relocatability asserts, tauri binary, artifact hotel-desktop-macos-aarch64 ~8.7MB). Pinned @tauri-apps/api ^2.11.1 / cli ^2.11.4 to the crate minor.
- Rule: when a CI step builds against Homebrew libs, add flags for every keg-only formula it links (icu4c, openssl@3, lz4 — check `brew info <f>` for "keg-only"); for npm/crate paired ecosystems (tauri), pin the npm side to the crate's major.minor whenever Cargo.lock moves, and grep both locks in the same review. A workflow whose later steps have never executed will fail once per step — budget dispatch iterations, not one.

## 2026-07-26r — SQL alias shadowed a real column; a failing never-green untracked test means concurrent WIP
- Trigger: hotel-timezone sweep (chrono::Local removal). Adversarial opus review of a green-gated diff found the new analytics alias `AS payment_date` shadowing the REAL `customer_ledger_payments.payment_date` column (user-supplied business date; 2 dev rows differ from created_at::date) — the alias made recording-date-vs-business-date invisible. Separately, `cargo test --all-features` failed mid-session on `tests/guests_rates_loyalty.rs` — an UNTRACKED file with fresh DEBUG eprintlns that did not exist during the previous green run (lib tests also changed 211→209 under us).
- Wrong: naming a derived SQL column after a concept without grepping the table's actual columns first; nearly treating the shared-tree test failure as a regression of this session's diff.
- Right: alias renamed to `created_date` (semantics preserved; the payment_date-vs-created_at display choice is a hotel-ops decision → task chip, rubric #3). For the test: `git status --short <file>` showed `??`, the suite had never run it before, and its domain had zero overlap with the diff — a never-green brand-new test cannot be a regression. Proved the rest green via `cargo test --all-features -- --skip <test_name>` (EXIT=0, 18 suites ok).
- Rule: before aliasing a computed SQL column, grep the table's DDL for that name — never alias a derivation to an existing column's name; and when the full suite fails mid-session in this repo, FIRST check whether the failing file is untracked/new since the last green run — if it has never been green, it is the concurrent session's WIP: skip it by name to verify everything else, and leave it to its owner.

## 2026-07-26s — first-ever tests for guests/rates/loyalty surfaced two severe, previously-invisible bugs
- Trigger: writing tests/guests_rates_loyalty.rs (zero prior coverage on all three domains). `services::rates::create_rate_plan` failed on the FIRST call with "column blackout_dates is of type jsonb but expression is of type text[]" — unconditionally, regardless of whether blackout_dates was Some or None, because `RatePlanCreateValues.blackout_dates: Option<Vec<String>>` binds as a Postgres TEXT[] parameter (sqlx's default Vec<T> encoding) against a `jsonb` column (repositories/rate.rs `create_rate_plan`, ~line 22-60). Separately, applicable_rate's base-price fallback returned price "0" for a room_type whose `base_price` was verified 50.00 in Postgres via psql AND via a raw `sqlx::query_scalar::<_, Decimal>` in the same test process.
- Wrong: assuming a decode mismatch would be the same root cause as the create bug, or a JSON-serialization artifact. Isolated it instead: `repositories/rate.rs:502` defines a PRIVATE `row_to_room_type(row: DbRow)` that SHADOWS the correct `models::row_mappers::row_to_room_type(&DbRow)` for every call in that file (`find_room_type`, `active_room_types`) — bare unqualified calls resolve to the file-local fn, not the imported module's, with no ambiguity error because Rust prefers the local item. That local fn tries `row.try_get::<String,_>("base_price")` then `.try_get::<f64,_>(...)` as a fallback — but sqlx cannot decode Postgres `numeric` as EITHER String or f64 (only as `Decimal`/`BigDecimal`); both attempts return `Err`, and `.unwrap_or_else(|_| "0".to_string())` silently swallows both failures, so `base_price`/`weekday_rate`/`weekend_rate`/`extra_bed_charge` are ALWAYS "0" regardless of the real stored value.
- Right: neither bug was fixed (out of scope for a test-writing task; flagged via spawn_task instead). The rate-plan tests were adapted to seed `rate_plans` via a direct SQL insert (bypassing the broken `create_rate_plan`) so the rest of the CRUD/priority/room-rate surface — all unaffected — could still get real coverage; the fallback-price assertion was dropped entirely (not asserted as either the correct OR the buggy value) per instructions to leave a bug-exposing scenario out rather than ship a test that encodes broken behavior as "expected".
- Rule: (1) when a service function fails on literally the FIRST/every call regardless of input shape, suspect a Rust-type-vs-column-type bind mismatch on a field whose Option-ness looks like it should matter but doesn't (Vec<String> vs jsonb is invisible until runtime — `cargo check` cannot catch it, sqlx's compile-time query checking is opt-in via `query!`/`query_as!` macros and this codebase uses plain `sqlx::query()` almost everywhere). (2) When two "same-named" mapper functions exist for one model (`grep -rn "fn row_to_<type>"` across the whole repo, not just the domain module), the LOCAL unqualified one wins silently in the file that defines it, even with the correct one imported and used elsewhere in the same file via a qualified path — this is a real Rust name-resolution trap, not a hypothetical. (3) Never trust a String/f64 double-fallback around a Decimal column read — sqlx numeric decode is Decimal-or-nothing; a `.unwrap_or_else(|_| "0")` after two failed non-Decimal attempts is not "best effort", it is "always zero, silently".
- Status update (2026-07-26, fix session): the shadowing bug is FIXED — the private `row_to_room_type` in repositories/rate.rs was deleted; `find_room_type`/`active_room_types` now call `row_mappers::row_to_room_type(&row)` (qualified), and the now-unused `DbRow`/`sqlx::Row` imports were removed. The fallback-price assertion was restored in tests/guests_rates_loyalty.rs (asserts 50.00, not 0) and passes live; full guests_rates_loyalty suite 9/9, clippy --all-features --all-targets clean. The blackout_dates jsonb bind in `create_rate_plan` was fixed concurrently by another session (binds `sqlx::types::Json`). Note rooms_queries.rs:74 still has its own private `row_to_room_type` — that one uses get_decimal/get_opt_decimal and is CORRECT (a duplicate, not a bug); do not "fix" it in a panic sweep.

## 2026-07-26r — git add <paths> + git commit swept in a concurrent session's staged deletions
- Trigger: committing 3 intended paths (2 new test files + docs/ongoing-dev.md) produced a 7-file commit — a parallel session had already STAGED 4 file deletions, and `git commit` commits the whole index, not just what this session added
- Wrong: assuming `git add <paths> && git commit` scopes the commit to those paths in a tree shared with concurrent sessions
- Right: the stray content was coherent here (the committed tracker line documented those very deletions), so the fix was amending the message to disclose it; the prevention is `git diff --cached --stat` BEFORE committing, and/or `git commit -- <paths>` (pathspec form commits ONLY those paths regardless of what else is staged)
- Rule: in this shared working tree, always run `git diff --cached --stat` before any commit and prefer `git commit -- <explicit paths>`; treat unexpected files in the commit result as concurrent-session staging, not a git malfunction

## 2026-07-26r — CI desktop artifact proven locally; three environment traps between artifact and running app
- Trigger: downloading run 30207504358's hotel-desktop-macos-aarch64 binary and running it locally with a scratch HOME.
- Wrong: (a) testing against `target/release/pgsql`, which can be a stale build-time copy of `src-tauri/pgsql`; sync `src-tauri/{pgsql,database}/` into `target/release/` before bare-binary tests. (b) Launching a GUI app from the sandboxed shell: everything boots, then exits because there is no WindowServer access. (c) Short curl timeouts on a machine running parallel cargo builds.
- Right: full first-boot verified: version guard, initdb, baseline+seed apply, seed passwords randomized, sidecar health, admin login, room query, and scheduled backup. Teardown must explicitly stop the embedded PostgreSQL process after killing the app.
- Rule: to test a desktop CI artifact locally: sync src-tauri resources into target/release, swap the binary (backup first), scratch HOME for isolation, disable the shell sandbox for the GUI, free port 5433 first (checking WHO owns it — docker publishes can shadow it), use ≥60s HTTP timeouts under build load, and always pg_ctl-stop the embedded PG after killing the app.

## 2026-07-26t — parallel vitest runs on this SSD mass-flake 5s-timeout tests; verify on a quiet machine before attributing
- Trigger: after the 2FA qrserver→qrcode.react swap, `bun run test` failed 9 tests (3 files); a re-run overlapping another vitest run failed 46 (17 files) — all in domains untouched by the diff
- Wrong: nearly treated tracked, green-committed test failures as regressions of the change under review
- Right: 41/46 were literal "Test timed out in 5000ms"; the 4 CustomerLedgerPage assertion failures passed 14/14 in isolation — overlapping vitest runs (this session + concurrent session) starve the 5s testTimeout (import phase 145s under load vs 4.9s quiet). Quiet-machine rerun: 776/776 green, exit 0
- Rule: never run two vitest suites concurrently in this tree; when a suite fails with timeout-heavy output, compare the run's import/environment Duration against a normal run and re-run ALONE before attributing failures — tracked-and-clean failing tests with "timed out" errors are load flakes until reproduced on a quiet machine

## 2026-07-27 — concurrent test fns in one binary share fixed fixture ids; login's double user-read made the race deterministic
- Trigger: new tests/rbac_profile.rs Scenario 10 (recovery-code login) failed twice at the same assertion — login WITHOUT a 2FA code succeeded despite the test having just set two_factor_enabled=true — then the fixture user vanished from the DB entirely. Scenario 10 had picked user id 920_010, which Scenario 4 ALREADY used as its actor id (rbac_profile.rs:327); cargo runs test fns concurrently, so Scenario 4's upsert_test_user reset two_factor_enabled=false between Scenario 10's seed and login's own two_factor_state re-query, and Scenario 4's final cleanup deleted the row. The panic output itself held the proof: the returned UserResponse had Scenario 10's username and an updated_at 9ms after created_at (the 2FA seed HAD landed), yet 2FA read false — because services::auth::login reads the user row twice (find_user_by_login at the top, two_factor_state later), and the reset landed between the two reads.
- Wrong: assuming a fresh fixed id is free by picking max-seen+1 from the scenarios I'd read; and initially suspecting the volume/concurrent-session ghosts before checking id collisions INSIDE the same file.
- Right: grep the whole test file for the candidate id (`grep -on "920_0[0-9][0-9]" tests/rbac_profile.rs | sort -u`) before claiming it; 920_010 and 920_011 were taken by Scenarios 4/5 as ACTOR ids (easy to miss — they aren't the scenario's headline user). Also: a service that reads the same row twice per request turns cross-test races into deterministic failures — useful diagnostic signature (row fields in the response predate the anomaly, branch behavior postdates it).
- Rule: when adding a scenario to a shared-fixture test file, grep the file for EVERY fixed id you intend to use (users, roles, actors) and pick one with zero hits; when a test fails with "state I just wrote reads as unwritten", list all ids the file uses BEFORE blaming storage or concurrent sessions — cargo test-fn concurrency inside one binary is the nearest suspect.

## 2026-07-27 — the SQLite removal dropped the loyalty bootstrap rows; a fresh PG V1 install has a dead loyalty module
- Trigger: CI run 30210501626 "Backend — PostgreSQL Smoke" failed on the two new
  tests/guests_rates_loyalty.rs loyalty scenarios — `enroll must succeed:
  Database("no rows returned by a query that expected to return at least one row")`.
  Both passed locally. `LoyaltyRepository::default_tier_id` does `fetch_one` on
  `loyalty_tiers WHERE is_active`, and a CI database (baseline + seed.sql)
  has ZERO tiers: `grep -rn loyalty_tiers database/postgres/{data,seed}.sql` is empty.
- Wrong: assuming a table that a routed, working feature reads is populated by the
  install set. The rows existed only in the deleted `database/sqlite/seed.sql`
  (`git log -S "INSERT INTO loyalty_tiers" -- "*.sql"` → bfb0562f1, the SQLite
  removal); the PostgreSQL seed.sql never carried them over. The dev DB has them
  because it was not a fresh database, so every local run passed.
  `loyalty_program_rules` id=1 was missing the same way — `update_rules` is
  `UPDATE ... WHERE id = 1`, so the whole rules surface was a silent no-op too.
- Right: restored program + 3 tiers + rules into `database/postgres/seed.sql`
  (verbatim values from the deleted SQLite seed.sql — restoration, not invented
  policy), and mirrored to hotel-desktop. The tiers MUST
  be inserted one statement per tier in rank order: `loyalty_tiers.id` is
  GENERATED ALWAYS AS IDENTITY and `rewards()` gates on `minimum_tier_id <=
  member.tier_id`, so id order encodes tier rank. A single `CROSS JOIN (VALUES ...)`
  insert produced silver=3/gold=1/platinum=2 on the first scratch install — which
  compiles, installs, and makes every reward visible to every member.
- Rule: a table read with `fetch_one` and never written by any handler is bootstrap
  data — grep `database/postgres/{data,seed}.sql` for it, and if empty check
  `git log -S "INSERT INTO <table>" -- "*.sql"` for a version that was dropped in a
  migration/engine removal. When seeding rows whose generated ids are compared with
  `<=`/`>=` anywhere in Rust, insert them one statement per row in rank order and
  assert the ids on a scratch install — set-based inserts do not preserve VALUES order.

## 2026-07-27 — user-module rework: route_access_policies is NAV, not API; a compat barrel is a safe machine-migratable seam
- Trigger: "rework the user module, it's too messy" at full scope (BE users/rbac split, `/rbac/users`→`/users` rename, delete the `HotelAPIService` compat layer across 44 files, decompose the 1051-line UserProfilePage).
- Wrong: assumed renaming backend `/rbac/users` would collide with seed.sql's self-validating `expected_route_access_policies` block (2026-07-15 lesson). It does NOT — those rows are FRONTEND nav routes (`('rbac','/rbac','Access Control','config',…)`), so backend API path renames never touch seed.sql. Check which layer a policy table describes before pricing a rename as schema-risky.
- Right: the compat class was a pure `static X = YService.Z` alias map, so the migration map was *derived from the file itself* (`re.findall(r'static (\w+) = (\w+)\.\w+;')`) rather than hand-written — 43 files rewritten mechanically, imports regenerated per-file from the set of services each actually used. Two traps that a hand-written map would have hit: (1) three aliases were RENAMES (`guestPortalVerify`→`GuestPortalService.verify`), caught only because typecheck flagged the non-existent members — always diff alias name vs target name (`awk '$3 != $5'`) before trusting a 1:1 assumption; (2) vitest files don't *import* the barrel, they `vi.mock` it with `HotelAPIService: {…}` as an object KEY, which no `HotelAPIService\.` regex matches — those factories must be regrouped per owning service separately, and `AuthContext.test.tsx` still failed afterwards because moving `getUserProfile` to `UsersService` left its `vi.mock('../api/auth.service')` stubbing a member the code no longer reads there.
- Rule: when deleting a backwards-compat aliasing layer, generate the rename map by parsing the layer instead of writing it out, then grep the test tree separately for the layer's name appearing as a mock KEY (`vi.mock(… () => ({ <Name>: {`) — call-site regexes never match those, and a stale service mock fails as a wrong-value assertion, not an import error. For any route rename, prove it live (new path 200 / old path 404 / unauth 401 / full CRUD roundtrip) — `cargo test` passes unchanged because nothing asserts URL strings.

## 2026-07-27 — `${PIPESTATUS[0]}` is a bash-ism that reads EMPTY in zsh; destructuring is the safe shape for arg→struct refactors
- Trigger: removing all 33 `#[allow(clippy::too_many_arguments)]` suppressions (166 call sites rewritten). The Tier-0 verification ran `cargo clippy … | grep … | head; echo "CLIPPY_EXIT=${PIPESTATUS[0]}"` and printed a bare `CLIPPY_EXIT=` — this shell is zsh, where the array is `$pipestatus` (lowercase) and `PIPESTATUS` is unset. The marker added to *satisfy* the 2026-07-26o "never end a verification command in a pipe" rule was itself meaningless.
- Wrong: assuming the documented fix (append an explicit exit marker) is shell-portable. An empty `EXIT=` renders as success to a skimming reader, which is worse than no marker at all.
- Right: redirect to a file and read `$?` directly — `cargo clippy … > /tmp/out.txt 2>&1; echo "EXIT=$?"` — or run the gate bare. The real evidence that a `-D warnings` clippy run passed is the `Finished` line: with `-D warnings` any warning aborts the build, so `Finished` cannot appear on a failing run.
- Also proved out on this task: for an N-args→param-struct refactor, put `let Struct { a, b, .. } = values;` as the FIRST line of the function and leave the body byte-identical. The `.bind()` chain then cannot drift (the 2026-07-26p bind/placeholder class), and a missed field is a compile error because destructuring is exhaustive. Verify the call-site half by machine: parse each call's positional args from `git show HEAD:<file>` and diff them against the named fields now present — 166/166 matched with zero mismatches, which no test suite could have established (audit writes in particular swallow their own errors and return `Ok(())`, so a mis-mapped field is silent).
- Rule: never write `${PIPESTATUS[0]}` in this repo — it is empty under zsh; use `cmd > file 2>&1; echo "EXIT=$?"`. For any positional→named-parameter refactor, destructure at the top of the callee and machine-diff every call site against its HEAD version rather than trusting compile+tests, which pass regardless of which argument landed in which field.

## 2026-07-27 — a security audit that reads the WORKING TREE inverts the risk it is measuring
- Trigger: 17-agent security evaluation. The opus synthesizer reported the unauthenticated `PATCH /api/bookings/{id}/pre-checkin` (rewrites guest name/email/IC by raw booking id, `// Public endpoint - no authentication required`) as "NOT live — the route is gone", downgrading it from blocker to a process note about not losing an uncommitted fix.
- Wrong: `grep pre-checkin hotel-app-be/src/routes/bookings.rs` returns nothing, so the auditor concluded the vulnerability was fixed. That grep read the working tree, which carried an uncommitted 102-deletion edit from a concurrent session. Production deploys from `master` (`.github/workflows/deploy.yml:18-20`, `workflow_run` → `branches: [master]`), and `git show HEAD:…/routes/bookings.rs` still has the route at :57/:221. `git log --all -S "pre_checkin_update"` returns only the commit that ADDED it — the removal is committed on no branch. The endpoint was live in production the entire time it was being reported as fixed.
- Right: the working tree is the most optimistic possible view of a deployment. For any claim about what is DEPLOYED, the source of truth is `git show <deployed-ref>:<path>`, and the deployed ref comes from reading the deploy workflow's trigger. An uncommitted security fix is indistinguishable from no fix, and it actively misleads the next reviewer in the *dangerous* direction.
- Also found in the same pass, worth reusing: the `ip_address` parameter threaded into `services::auth::login` is used correctly at `:228` (`store_refresh_token`) while all 8 `AuditLog::log_login_*` calls in that same function pass literal `None, None` — so `refresh_tokens.ip_address` is populated but `audit_logs.ip_address` is always NULL for logins. A dead argument at SOME call sites of a function whose OTHER call sites populate it is invisible to compiler, clippy and tests; grep the call sites for `None,` whenever a nullable forensic column reads empty.
- Rule: when auditing security posture, state explicitly which ref you read. Verify every "this is already fixed" claim with `git show HEAD:<path>` (and `git log --all -S "<symbol>" -- <path>` to prove the fix was ever committed) before downgrading a finding — never from a working-tree grep. In this shared, concurrently-edited tree, run `git status --short` at the start of any audit and treat every modified file as "the tree lies about production".

## 2026-07-27 — a concurrent session refactored a file mid-edit; attribute gate failures by file before believing them
- Trigger: implementing observability prereqs (metrics counters, login-audit IP threading). An `Edit` to `core/middleware.rs::check_permission` failed with "String to replace not found" minutes after I had read that exact text — the other session had just moved the `:manage` implication down into `AuthService::check_permission`. Later `cargo clippy --all-features -- -D warnings` exited 101 with 7 errors, and `git diff --stat` grew from 10 files to 43 (MyBookingsPage/InvoiceModal deleted, rbac/ekyc rewritten) during the task.
- Wrong: nearly treating the 7 clippy errors as mine and "fixing" them. All 7 were in `src/repositories/ledger.rs` (unused `amount_param`/`tax_amount_param`, dead `CHECK_LEDGER_VOIDED_QUERY`) — a file I never opened, belonging to the other session's in-flight work. Also nearly committed: several files (`services/auth.rs`, `core/middleware.rs`, `services/passkey.rs`) now contain BOTH sessions' uncommitted changes interleaved, so any `git commit` of those paths ships their unfinished refactor under my message.
- Right: attribute every gate failure to a file before acting — `grep -E "^\s+-->" clippy.txt | sort -u` and compare against the set of files you actually touched. A failing shared-tree gate is not evidence about your diff. And a failed `Edit` on text you just read is a *useful signal* that the file moved under you, not a tool glitch: re-read and re-apply rather than forcing a `replace_all` or reconstructing from memory.
- Rule: in this tree, before editing a backend file, note whether `git status --short` already lists it as modified — if it does, another session owns in-flight changes there and your edits will be inseparable from theirs at commit time. Prefer new files (a new module) over edits to hot files when possible; my `core/metrics.rs` stayed cleanly attributable while my one-line edits to `auth.rs`/`middleware.rs` did not. Never `git commit -- <path>` a file that was already dirty before you touched it without diffing it hunk by hunk first.

## 2026-07-27 — nav-route removal: the sidebar is CODE-driven, the RBAC panel is DB-driven; they are two different surfaces
- Trigger: deleting the legacy `/my-bookings` guest page. The removal plan justified its `route_access_policies` DELETE patch as "otherwise the My Bookings nav item keeps rendering on existing deployments, producing a 404". An adversarial DB-lens reviewer refuted that: `NavigationTabs.tsx:53-59` builds the sidebar from `navigationRouteDefinitions` (the CODE registry in `src/navigation/routeRegistry.tsx`), so deleting the registry entry removes the item everywhere immediately, regardless of the database.
- Wrong: treating `route_access_policies` as the source of the sidebar. It is not. Also wrong in the same plan: claiming a leftover row "accrues one app.invalid_data_quarantine row per seed.sql re-apply" — `seed.sql:16-27` is a DO block that RAISEs on any re-apply against an existing V1 database, so seed.sql runs exactly once and can accrue nothing.
- Right: `route_access_policies` feeds exactly one visible surface — the RBAC admin panel at `features/admin/components/rbac/RolesTab/NavigationAccessSection.tsx:65-67`, which builds `navByCategory` from the `routePolicies` prop (the DB list), not from the registry. A stale row therefore shows a phantom toggle in Access Control → Roles → Navigation Access and nowhere else. That makes the patch admin-UI hygiene, not a functional fix — and it means BOTH deploy orders are benign (`canAccessNavigationRoute` returns false when `accessControlled && !policy`, so an old FE with a patched DB hides the item cleanly).
- Also verified this pass: `permissions.id` is the target of TWO `ON DELETE CASCADE` FKs — `role_permissions_permission_id_fkey` (0001_v1_baseline.sql:9103) AND `user_permissions_permission_id_fkey` (:9423). Deleting a permission row silently drops per-ROLE and per-USER grant history, which seed.sql:947-948 explicitly preserves. A `navigation_*` permission that seed.sql never INSERTs (only lists in `expected_system_permissions`) is inert — leave it.
- Rule: before writing a patch that deletes a `route_access_policies` row, state which surface actually renders the thing you are removing — grep `navigationRouteDefinitions` (code/sidebar) vs `routePolicies` (DB/RBAC panel); they are not the same list. Never DELETE FROM permissions in a patch without first checking every `REFERENCES public.permissions(id)` in the baseline — there are two cascades, not one. And for a data-only patch, the convergence proof is `pg_dump --data-only` on the affected tables, NOT `--schema-only` (which is trivially empty and proves nothing).

## 2026-07-27 — a dead validation guard placed after unconditional pushes; verify a bugfix's test by reverting the fix
- Trigger: `update_customer_ledger` (repositories/ledger.rs:479) built a `Vec<String>` of SET clauses, then pushed `updated_by`/`updated_at` UNCONDITIONALLY before checking `if updates.len() < 2`. That length can never be < 2, so an all-`None` `CustomerLedgerUpdateRequest` silently ran an UPDATE touching only the audit columns and returned 200 instead of the intended 400. Found only because a characterization test called the service with an empty request.
- Wrong: writing a "did the caller supply anything" guard AFTER appending machine-generated clauses to the same collection being counted. `cargo check --all-features`, clippy `-D warnings`, and the whole suite were green on it — a dead guard is invisible to all three, because it compiles and is simply never taken.
- Right: the guard belongs where the collection still holds ONLY caller-supplied entries — `if updates.is_empty()` immediately before the `updated_by`/`updated_at` pushes. Both siblings already do exactly this (`repositories/bookings/credits.rs:470-474` checks `is_empty()` then pushes `updated_at`; `repositories/payment.rs:1178` checks `is_empty()`), so ledger.rs was the lone divergent copy — grep the siblings for the same guard before designing a fix. Also confirmed the fix cannot regress the only production caller: `CustomerLedgerPage.tsx:1246` pre-populates the edit form with the ledger's existing values, so a real PATCH always carries repo-handled fields.
- Rule: any `if <collection>.len() < N` / `.is_empty()` validation must be placed before ANY unconditional push to that collection — count caller intent, never a collection you have already appended to yourself. And when adding a regression test for a bugfix, PROVE it is not vacuous: revert the fix (`git apply -R`) with the test in place and confirm it fails at your assertion, then restore. A test written after the fix that was never seen red is not evidence.
- Corollary (verification hygiene, cost me two false starts here): when the shared working tree is mid-edit by a concurrent session, `cargo check` fails on THEIR half-finished work (here `core/mod.rs` imported `require_super_admin_helper` after its definition was deleted from `core/middleware.rs`) — verify in `git worktree add <scratch> HEAD --detach` + `git apply` of your own diff, which isolates your change from theirs. Confirm ownership first with `git show HEAD:<path>` on both files. Also: `DATABASE_URL` for live tests must come from `set -a; . hotel-app-be/.env` — do not guess `postgres:postgres` (fails 28P01), and note that .env has an unquoted value that makes the shell emit a harmless `command not found: Inn` line.

## 2026-07-27 — `git diff --cached` before commit is NOT enough in this shared tree; use a temp-index plumbing commit
- Trigger: committing a single verified one-hunk ledger fix. `git diff --cached --stat` printed exactly 1 file, and the very next `git commit` in the SAME bash invocation produced a 5-file commit (e4b53105) — the concurrent session staged `.claude/CLAUDE.md`, `.claude/settings.json`, `.mcp.json`, `.claude/refs/codegraph.md` in the milliseconds between the check and the commit.
- Wrong: believing 2026-07-26r's remedy ("run `git diff --cached --stat` BEFORE committing") is sufficient. It is a snapshot, not a lock — there is no gap small enough to be safe when another session is actively `git add`ing, not even within one shell command. Also wrong for this case: `git commit -- <paths>` (the other half of that lesson), because the pathspec form commits the WORKING-TREE version of those paths, and here the working tree copy of the same file contained the other session's unfinished `net_amount` rewrite of the very function being fixed.
- Right: build the commit with plumbing against a TEMP index, so the real index cannot contaminate it and the other session's staging survives untouched:
  `export GIT_INDEX_FILE=<scratch>/tmpidx; git read-tree <parent>; git update-index --cacheinfo 100644,<blob>,<path>; TREE=$(git write-tree); NEW=$(git commit-tree $TREE -p <parent> -F msg); unset GIT_INDEX_FILE; git update-ref refs/heads/<branch> $NEW`
  Get `<blob>` from `git hash-object -w <file>` on the exact content you verified. Confirm afterwards that the other session's files returned to `A `/`M ` staged state and that the discarded commit is on no branch (`git branch --contains <sha>` empty).
- Rule: in this tree, never produce a commit by staging-then-`git commit` when `git status` shows another session's dirty/staged files. Construct it with `GIT_INDEX_FILE` + `commit-tree` + `update-ref`, which is atomic with respect to their index. And when your fix lands in a file the other session is concurrently rewriting, commit the blob you actually TESTED (parent's version + your edit), never the working-tree file — verify the staged blob's hash matches the hash from the diff you verified earlier in the session.

## 2026-07-27 — `clippy --all-targets` is NOT a superset of plain clippy: it HIDES dead_code in the bin target
- Trigger: commit 1762d5fe0 (/my-bookings removal) turned CI red. Local gate was `cargo clippy --all-features --all-targets -- -D warnings` → EXIT 0. CI runs `cargo clippy --all-features -- -D warnings` (no `--all-targets`) → EXIT 101, three `function ... is never used` errors in `src/repositories/bookings/lifecycle.rs`, all reported against the **bin** target (`could not compile hotel-app-be (bin "hotel-app-be")`).
- Wrong: assuming `--all-targets` is strictly stricter because it lints more targets, and treating a green `--all-targets` run as covering CI. For `dead_code` it is the opposite — adding targets adds *uses*, which SILENCES the lint. `hotel-app-be` has both a lib (`src/lib.rs`) and a bin whose `main.rs` re-declares the whole module tree with `mod core; mod handlers; ...` rather than `use hotel_app_be::*`. The bin therefore compiles its own private copy of every module, where nothing is externally reachable and dead_code applies in full; in the lib the same items are `pub` and exempt. `tests/booking_service.rs:1367` calls `reactivate_booking_handler`, so under `--all-targets` that use exists and the lint stays quiet.
- Right: run the CI command VERBATIM, and prove it in a clean `git worktree add --detach <sha>` — the shared working tree carried a concurrent session's WIP whose own clippy error (`services/ledgers.rs:118` collapsible-if) masked the real failure and made the first local reproduction attempt meaningless. Fix: delete the two functions with zero references anywhere (`delete_booking_handler`, `manual_checkin_handler` — 518 lines of pre-refactor duplicates superseded by `services::bookings::void_booking`/`manual_checkin`), and `#[allow(dead_code)]` the third. Use `allow`, NOT `expect`: the lint does not fire for the lib target, so an `expect` would be an unfulfilled expectation there and fail under `-D warnings`.
- Rule: never substitute a "stricter-looking" flag set for the one CI runs — copy `.github/workflows/ci.yml`'s command string character for character, and run BOTH if you also want `--all-targets` coverage (they catch disjoint things: `--all-targets` finds `items_after_test_module`, plain finds bin-target `dead_code`). In this repo that means `cargo clippy --all-features -- -D warnings` AND `cargo clippy --all-features --all-targets -- -D warnings`. Any glob re-export chain (`pub use lifecycle::*` → `pub use crate::repositories::bookings::*`) makes it non-obvious which items the binary actually reaches, so removing a single call site can strand unrelated functions.

## 2026-07-27 — customer_ledgers.net_amount is INSERT-only; a derived SET clause must not read the columns it is updating
- Trigger: wiring the 13 silently-discarded fields on `CustomerLedgerUpdateRequest` (repositories/ledger.rs `update_customer_ledger`). Grepping the baseline for `net_amount` found it is written ONLY by `generate_folio_number()`, a BEFORE **INSERT** trigger, and only `IF NEW.net_amount IS NULL`. The BEFORE UPDATE trigger (`update_customer_ledger_timestamp`) sets `updated_at` and nothing else.
- Wrong: two traps here, neither caught by check/clippy/tests. (1) Wiring `tax_amount`/`service_charge` alone would have traded one silent-money bug for another — `net_amount` would keep its creation-time value forever. This was ALREADY latent for `amount`, which was wired all along, so every amount edit had been leaving `net_amount` stale. (2) The obvious fix `SET net_amount = amount - COALESCE(tax_amount,0) - COALESCE(service_charge,0)` is WRONG: SQL evaluates every SET expression against the **OLD** row, so it would compute the new net from pre-update inputs and silently store a wrong total.
- Right: emit each of the three inputs as either its own `$N` placeholder (when this request changes it) or a bare column name (when it does not) — `net_amount = $13 - COALESCE(tax_amount, 0) - COALESCE($20, 0)`. Reusing a placeholder adds no bind, so clause/bind order is untouched; the bare column deliberately reads the old value, which is correct for an untouched input. Push the derived clause AFTER the `updates.is_empty()` guard so it never counts as caller intent (2026-07-27 dead-guard lesson). Proven live: an amount-only update to 300 on a row with tax=20/service=8 yields net=272.
- Rule: before wiring any column into an UPDATE, grep the baseline for triggers/generated columns that DERIVE from it and check whether the deriving trigger is INSERT-only — `BEFORE INSERT` + `IF ... IS NULL` means the value is frozen at creation and every later edit silently desyncs it. When adding a derived SET clause, never reference a column the same statement also assigns; use that column's placeholder instead. And prove the regression test is not vacuous by reverting ONLY the repository hunk (`git apply -R`) and confirming it goes red — here it failed with `BadRequest("No fields to update")`, the exact pre-fix symptom.

## 2026-07-27 — dead-code sweep: `main.rs` re-declares every module, so the BIN is the dead-code detector and `tests/` are invisible to it
- Trigger: "search for allow dead code, clean every dead code" — 73 `#[allow(dead_code)]`/`#[allow(unused_imports)]` in `hotel-app-be/src`. Stripping all of them and compiling surfaced ~170 warnings.
- Wrong: assuming `pub` items in a lib crate are exempt from `dead_code` (they are), and therefore that a warning means the item is unreachable. `src/lib.rs` and `src/main.rs` BOTH compile the same sources: main.rs does `mod core; mod handlers; …` rather than `use hotel_app_be::…`, so the crate is built twice and in the **bin** copy `pub` is no shield. Every warning came from the bin (`hotel-app-be (bin) generated 168 warnings`; the lib generated 1). Consequence: an item used ONLY by an integration test in `tests/` (which links the LIB) is reported dead by the bin — deleting it breaks the test suite while `cargo check` stays green. `tests/promotion_pricing.rs`, `tests/rate_limiter_tests.rs`, `tests/booking_service.rs` and `tests/audit_analytics_settings.rs` each own such items.
- Right: treat the double compile as a free dead-code detector, but gate every deletion on a `tests/` cross-check. Name-grep is not enough (`check`, `create`, `exists`, `limit`, `fixed`, `room_number` all collide); the decisive greps are the QUALIFIED forms — `grep -rho "BookingRepository::[a-z_]*" tests/` for associated fns, and the `use hotel_app_be::…` import list for modules. Extract the authoritative item list from `cargo check --message-format json` filtering `is_primary` spans, not the rendered text (rustc renders whole impl blocks with elisions, so context lines look like findings). Final verification is `cargo check --all-features --all-targets`, which compiles `tests/` and turns a wrong deletion into a compile error.
- Also burned: a brace-matching item remover must special-case `const`/`static` — cutting "to the first line containing `;`" destroys a multi-line SQL const at the first semicolon INSIDE the string, leaving an orphaned string tail that fails 60 lines later with a bogus "unknown prefix" lex error (`services/audit.rs::AUDIT_LOGS_MIGRATION`).
- Rule: in this repo, a `dead_code` warning is evidence about the BIN only. Before deleting, run the qualified `Struct::method` / `use hotel_app_be::` grep over `tests/`; if a test owns it, keep it with a one-line `#[allow(dead_code)] // used by tests/<file>.rs` rather than deleting. Never conclude "unused" from the un-qualified symbol name.

## 2026-07-27b — three sessions in one tree: gate failures, test failures and even your own commits belong to someone else
- Trigger: same sweep. `cargo check` broke mid-task on `modules/guest_booking/service.rs` (missing `complimentary_*` fields) — files written seconds earlier by another session mid-feature. Later `cargo clippy` failed only on `modules/teams/repository.rs::team_ids_led_by`, in a directory `git status` showed as `??` (untracked, created 04:42 during my session). Then `git status` went CLEAN: commits `6366258f2` and `24042f1dd` (154 files) had absorbed all of my uncommitted work under another session's messages.
- Wrong: nearly deleting `team_ids_led_by` (an untracked module's WIP — its caller was minutes away) and nearly attributing 4 integration-test failures to the sweep. `rbac_profile` failed 3/11 with `user_roles_user_id_fkey … Key (user_id)=(920011) is not present in table "users"`; alone it passed 11/11. `ps aux` showed the other session running the IDENTICAL test from a pristine-HEAD worktree.
- Right: attribute before acting. `git status --short <path>` distinguishes your edits (` M`) from another session's new module (`??`) — never delete dead code from an untracked directory, the feature is still being written. For test failures, `pgrep -f "cargo test"` first; fixed-id fixtures against one shared dev DB make concurrent runs collide deterministically. A crashed raced run also POISONS the DB: `tests/rooms_housekeeping.rs` cleanup deletes `users` but leaves `room_history` rows whose `changed_by` FK does not cascade, so every later run fails at `DELETE FROM users` until those rows are removed (verify with `SELECT … FROM room_history WHERE changed_by = <fixture id>` and compare `created_at` to the crash time — timestamps predating your change prove it is not yours).
- Rule: at the start of any repo-wide sweep run `ps aux | grep "[c]argo\|[c]laude"` and `git status --short`; treat every already-dirty or untracked path as owned by another session and leave it alone. Attribute each gate failure to a FILE and check whether you touched it before believing it. Expect your work to be committed by someone else — verify it landed with `git show <commit> --stat` and grep for your deletions rather than assuming an empty `git status` means your changes were lost.

## 2026-07-27c — CORRECTION to 2026-07-26e: "room_history cascades" is true for room_id ONLY; a whole-table function stamps the actor onto non-fixture rows
- Trigger: `postgres_sync_all_room_statuses_respects_housekeeping_states` failed with `23503 room_history_changed_by_fkey`, `Key (id)=(980004) is still referenced`. Lesson 2026-07-26e had asserted "room_history cascades, so it needs no delete", and `cleanup_room` (tests/rooms_housekeeping.rs:260-266) repeats that claim in a comment.
- Wrong: reading "room_history cascades" as a property of the TABLE. The baseline defines two FKs with different semantics — `room_history_room_id_fkey … ON DELETE CASCADE` (0001_v1_baseline.sql:9286) but `room_history_changed_by_fkey FOREIGN KEY (changed_by) REFERENCES public.users(id)` with NO action (:9278). Deleting the room reaps its rows; deleting the ACTOR is blocked by them. Also wrong: diagnosing this as mere crash residue. Every test in the file cleans rooms BEFORE the actor, so fixture-room rows can never block — the leak is that `sync_all_room_statuses` loops over EVERY active room (`WHERE r.is_active AND r.status IN ('available','occupied','reserved')`) and passes `p_user_id` into `update_room_status`, which writes one `room_history` row per reconciled room. Any drifted REAL room outside the 980_xxx fixture set therefore pins the actor, and nothing keyed on `room_id` will ever reach it.
- Right: added `DELETE FROM room_history WHERE changed_by = $1` to `cleanup_actor`, before `DELETE FROM users`. Because the same helper runs pre-test as well as post-test, this also self-heals residue from earlier crashed runs. Proven non-vacuous per the 2026-07-27 revert discipline: drifting ONE real room (`UPDATE rooms SET status='occupied' WHERE id=6` — no current booking, so sync corrects it straight back to 'available', net-zero) reproduced the user's exact error on the HEAD version (exit 101, 5 passed/1 failed) and passed twice with the fix, self-healing `stranded_rows=1`/`orphan_actor_rows=1` left by the failing run. Sibling audit: `cleanup_actor` is the only `DELETE FROM users` in that file; `tests/booking_service.rs` has three user-deleting cleanups and no `room_history` delete but has NO gap, because its history rows come only from the `sync_room_status_with_booking` trigger, which calls `update_room_status(..., NULL, ...)` — confirmed by every 993xxx row in the dev DB having `changed_by IS NULL`.
- Rule: never state "table T cascades" — cascade is per-FK. Before deleting a fixture user, list that user's inbound FKs with `grep -E "REFERENCES public.users\(id\)" 0001_v1_baseline.sql | grep -v "ON DELETE"` (38 of them today) and check which the test can populate. And when a test calls a function whose scope is the WHOLE table (`sync_all_*`, bulk reconcilers, night audit), its writes are NOT confined to fixture ids — cleanup must key on the actor/fixture column, not just `room_id`/`booking_id`.

## 2026-07-27d — zsh does NOT word-split unquoted `$var`, so a loop over flag-strings silently mis-invokes the command
- Trigger: verifying the guest-portal credits feature with `for t in "--lib" "--test guest_portal_credits"; do cargo test --all-features $t; done`. Every entry containing a space reported EXIT=1 with no test output, which reads exactly like a failing suite. Run directly, `cargo test --all-features --test guest_portal_credits` passed 8/8.
- Wrong: assuming the bash behaviour where unquoted `$t` splits on IFS. zsh passes `$t` as ONE argument, so cargo received the literal `--test guest_portal_credits` (with the space) as a single flag and errored out before running anything. Nearly attributed three "failures" to my own change.
- Right: never iterate flag-strings. Either write the invocations out one per line (what finally proved it: booking_service 14/14, guest_portal_postgres 1/1, promotion_pricing 3/3, guest_portal_credits 8/8), or use an array with `${(z)t}` / `"${t[@]}"` if a loop is genuinely needed. A cargo target that fails to BUILD prints errors; a bare `EXIT=1` with an empty `test result` line means the command never ran.
- Rule: in this repo's zsh, treat any `$var` holding more than one word as a single argument. When a verification loop reports failures with no accompanying test output or compiler error, re-run one invocation by hand before believing it — same family as the 2026-07-27 `${PIPESTATUS[0]}` and 2026-07-26o failed-glob lessons: the harness lied, not the code.

## 2026-07-27e — `Array.prototype.at` passes vitest but fails `tsc --noEmit`; FE gates disagree on lib level
- Trigger: new `BookingsSection` search tests used `mocks.bookings.mock.calls.at(-1)![0]`. `bun run test` passed 800/800 and `bun run lint` exited 0, but `bun run typecheck` failed with TS2550 ("Property 'at' does not exist ... change 'lib' to 'es2022' or later").
- Wrong: treating a green vitest run as evidence the code typechecks. Vitest transpiles without type information, so any API newer than the configured `lib` runs fine at test time and only the separate `tsc --noEmit` gate catches it.
- Right: replaced with index arithmetic behind a named helper (`calls[calls.length - 1][0]`) rather than widening `lib` in tsconfig for a test-only convenience.
- Rule: run `bun run typecheck` after adding FE tests, not just `bun run test` — they are independent gates and the test runner is the weaker one. Avoid `.at()`, `Object.groupBy`, `findLast` and friends in this project until tsconfig's `lib` is raised.

## 2026-07-27 — CORRECTION to 2026-07-15: seed.sql QUARANTINES obsolete system permissions, it does not DELETE them
- Trigger: retiring the `ekyc:verify` permission alongside the legacy `PATCH /ekyc/verifications/{id}` endpoint. The 2026-07-15 lesson states a `DELETE FROM permissions WHERE is_system_permission AND NOT EXISTS (in expected_system_permissions)` "silently removes them"; I began designing the patch around that behaviour.
- Wrong: `grep -n "DELETE FROM permissions" database/postgres/seed.sql` returns NOTHING. There is no such statement anywhere in seed.sql. What exists (seed.sql ~line 971) is an INSERT into `app.invalid_data_quarantine` selecting system permissions absent from `expected_system_permissions`, with the comment "They are intentionally retained so the one important database never loses authorization history." Obsolete permissions are copied to quarantine and LEFT IN PLACE.
- Right: `expected_system_permissions` is a MUST-EXIST checklist — the validation block RAISEs when an expected record is missing, and does nothing about extras beyond quarantining them. Removing a permission from a fresh install needs both the `permissions` INSERT row and the `expected_system_permissions` entry removed. Legacy databases are rebuilt rather than mutated in place.
- Also: the permissions INSERT's last VALUES row carries no trailing comma, so deleting it requires removing the comma from the new last row.
- Rule: never trust a lessons.md claim about what a SQL bootstrap file does without re-grepping the statement it names — this file is large, self-validating, and has been rewritten since several lessons were written. Before removing any seeded row, grep seed.sql for BOTH the seed INSERT and every `expected_*` checklist that names it; before deleting any `permissions` row anywhere, check `REFERENCES public.permissions(id)` in the baseline (there are two ON DELETE CASCADE FKs) and quarantine the grants first.

## 2026-07-27 — a wedged `systemctl reload` poisons every later reload, so the "known-good control" lies
- Trigger: deploy of 553429e74 failed with `Job for caddy.service failed`; `caddy validate` reported "Valid configuration" and all three app containers were healthy. Root cause: deploy.sh's new site block wrote an access log to `/var/log/caddy/saliminn-access.log`, and `caddy validate` — run as ROOT immediately before the reload — PROVISIONS the config and creates that sink, so the file landed `root:root 0600`. Caddy's service runs as the `caddy` user, could not open it, and `systemctl reload` HUNG for systemd's full 90s TimeoutStartSec instead of failing fast. Proof: `chown caddy:caddy` the file → reload with the identical config returns rc=0 in 0s.
- Wrong: mid-diagnosis I concluded "reload hangs with the known-good config too, so the log block is exonerated — this is a pre-existing defect". That control was contaminated. `caddy.service` was still in `SubState=reload-notify` from the previous hung attempt, and while systemd sits there EVERY subsequent `systemctl reload` queues behind it and times out identically, regardless of config. The journal proved the opposite: at 21:59:32 the baseline reload logged `Reloading → Reloaded` (success), and only the very next reload — the one carrying the log block — began the timeout cascade.
- Right: before A/B testing a service reload, assert the unit is `active/running` (not `reloading`/`reload-notify`) and wait for it to settle, or the control run is meaningless. Read `journalctl -u <unit>` for the `Reloading → Reloaded` vs `Reload operation timed out` pairs rather than trusting one timed invocation. Also: bound every production probe to a SINGLE reload with an explicit `timeout`; my first attempt chained three reloads inside one SSH call, overran the tool's 5-minute cap, was SIGTERMed before its restore trap ran, and left the unit cycling through timeouts for minutes (the site stayed 200 — Caddy keeps serving its active config when a reload fails).
- Rule: `caddy validate` run as root creates any `output file` log sink root-owned — pre-create the file as the service user, or use stderr and let journald handle it (what the fix in fb4c4c37f does). More generally: when a reload "hangs" rather than errors, suspect a permission the validator holds but the service does not. And never trust a control run taken while the unit is mid-reload.
## 2026-07-27 — `cargo test` reports green with EVERY money test skipped; CI clippy omits `--all-targets`; and a shared tree makes tests compile against another session's uncommitted structs
- Trigger: booking/payment/ledger rework. Built a characterization net for 10 money functions that had ZERO test callers (record_payment, approve_payment, reject_payment, capture_paypal_payment, refund_deposit, void_ledger, create_ledger_reversal, update_ledger_payment, delete_ledger_payment, update_customer_ledger).
- Wrong, and this invalidates every past "tests pass" claim about money code: 10 of 13 files in `hotel-app-be/tests/` gate on `DATABASE_URL` and silently `return` when it is unset (`grep -rln DATABASE_URL tests/` = 10, `ls tests/*.rs` = 13). `cargo test --all-features` therefore EXITS 0 having run none of them. With `DATABASE_URL` set the same suite runs 519 tests. An exit code is not evidence here; the run count is.
- Also wrong: assuming CI's clippy matches the strict local invocation. CI runs `cargo clippy --all-features -- -D warnings` (ci.yml:129) with NO `--all-targets`, so an entire error class is invisible to CI — confirmed by finding three genuinely dead fns (`delete_booking_handler` lifecycle.rs:1912, `manual_checkin_handler` :2075, `reactivate_booking_handler` :2434; live chain is routes→handlers→`services::bookings`) that only `--all-targets` reports. Corollary: a local `--all-targets` failure is not necessarily a CI failure, and a CI pass is not proof of dead-code cleanliness. Decide which bar you are claiming and name the exact command.
- Also wrong: authoring tests in the shared main tree while another session had 60 uncommitted `src/` files in flight. `tests/ledger_characterization.rs` compiled there against that session's *modified* `CustomerLedgerUpdateRequest`, then failed `E0063` (6 missing fields: booking_id, guest_id, folio_type, transaction_type, posting_date, transaction_date) the moment it was moved to a clean worktree based on `origin/master`. The struct has no `Default`, so `..Default::default()` is not an escape hatch.
- Right: every money-test gate must (a) export `DATABASE_URL` and (b) assert the RUN COUNT (a real full suite is ~513 passed / 0 failed / 10 ignored; ~209 means only the lib tests ran) — not merely `EXIT=0`. **CORRECTED 2026-07-27, see the entry below: do NOT use "zero `Skipping` lines" as the check — that `eprintln!` goes to stderr, which libtest captures and SUPPRESSES for passing tests, so a fully-skipped run shows zero `Skipping` lines and green, indistinguishable from a real one.** Author tests in an isolated worktree, not a tree with concurrent uncommitted work. And for fix-gated tests asserting not-yet-implemented behavior, `#[ignore]` alone rots: a CI step must run `-- --ignored` and FAIL when one PASSES (a stale `#[ignore]` means the fix landed and the attribute must go). Added exactly that to the `backend-postgres-smoke` job, which already sets `DATABASE_URL` at job level — note the non-ignored characterization tests DO run in CI there, so "invisible in CI" applies only to the ignored set.
- Rule: never claim a money path is tested on the strength of a green `cargo test` — set `DATABASE_URL`, count the tests that ran, and grep the output for `Skipping`. Before quoting a clippy verdict, check whether CI passes `--all-targets` (it does not here) and state which command you ran. Never write tests in a tree another session is mid-refactor in: a struct literal that compiles today fails in a clean checkout, and the compiler is the only thing that catches it.

## 2026-07-27 — adversarial review caught two test assertions that pinned money bugs as CORRECT; self-verifying agents had passed both
- Trigger: three sonnet agents wrote the money characterization net, each self-verifying (clippy clean, tests green, fixtures idempotent). A fresh-context opus reviewer instructed to REFUTE returned FAIL with 12 findings, 2 of them money-critical.
- Wrong: (a) a test asserted a 50.00 deposit refund SUCCEEDS on a booking with `keycard_deposit_amount = 0` and zero completed payments — verified in src that `services/payments.rs:390-419` checks only `deposit_amount > 0` and `repositories/payment.rs:785-829` checks only "already refunded", so nothing bounds the payout. The test blessed an unbounded cash refund as expected behavior. (b) another asserted, with an approving comment ("a reversal is inserted already fully self-paid"), that a reversal row carries `paid_amount = amount, status = 'paid'` — but `get_ledger_summary` (repositories/ledger.rs:896-909) sums over all non-void rows with NO `transaction_type` sign handling, so reversing a 400.00 charge doubles reported billings to 800.00 and INVENTS 400.00 of collected cash. Measured live: baseline 10830.00 → 11630.00.
- Also caught: a cross-file fixture collision (`rooms.id = 940_302` shared with `booking_service.rs:946`) that the writer missed because it grepped only its own file — the exact failure mode of the 2026-07-27 fixed-id lesson, one scope level up; and a writer's false claim that `clippy --tests -D warnings` passed (it exited 101 on a `bool_assert_comparison`).
- Right: characterization tests must assert CORRECT values, never current-buggy ones. Where behavior is wrong, write the correct assertion and `#[ignore]` it with a reason naming the pending fix. Verify `#[ignore]` DIRECTIONALITY by running `-- --ignored` and reading each failure message: it must fail at its intended assertion, not at fixture setup. And clean up fixtures BEFORE any assertion that can panic, or an `--ignored` run leaks rows into the shared dev DB (measured: 31 leaked rows including a booking stuck in `checked_out`, versus 0 for the one file that cleaned first).
- Rule: never let the agent that wrote a test certify it — budget a fresh adversarial reviewer for any test net over money code, and have it re-derive every money assertion against the authoritative definition (`billable_total()` = total_amount + tourism_tax_amount + extra_bed_charge, models/payment.rs:285-287). For fixed fixture ids, grep `tests/` repo-wide PER TABLE, never just the file you are editing. "All my tests pass" from the author is the weakest possible evidence that a test net is correct — a test that pins a bug passes forever.

## 2026-07-27 — CORRECTION to 2026-07-26e: deleting a test ACTOR is FK-pinned by room_history.changed_by
- Trigger: `cargo test --all-features` in a clean worktree failed one pre-existing test — `tests/rooms_housekeeping.rs:215` panicked with Postgres 23503, `update or delete on table "users" violates foreign key constraint "room_history_changed_by_fkey"`, `Key (id)=(980004) is still referenced from table "room_history"`. Two residue rows were confirmed live: `SELECT count(*) FROM room_history WHERE changed_by = 980004` = 2.
- Wrong: the 2026-07-26e lesson says "room_history cascades, so it needs no delete". That is true only for the `room_history.room_id` → `rooms` edge. `room_history.changed_by` → `users` is a SECOND edge with no cascade, and the room-status triggers write those rows behind the test's back — so any teardown that deletes its actor user fails as soon as one run has left history behind. The failure is order-dependent and looks like a flake: the first run on a clean DB passes, every later run fails.
- Right: teardown must `DELETE FROM room_history WHERE changed_by = $1` before `DELETE FROM users WHERE id = $1`. Fixed in `tests/rooms_housekeeping.rs`. Note this failure is invisible until the residue exists, so "it passed locally" proves nothing on a persistent shared dev DB.
- Rule: when a test creates an actor user, enumerate EVERY table with an FK to `users` that a trigger might write (room_history.changed_by, audit_logs.user_id, room_status_change_log, user_roles) and delete them before the user — do not reason from one FK edge of a table to the table as a whole. A table can cascade on one parent and pin on another.

## 2026-07-27 — a verification agent FABRICATED a pass; "zero Skipping lines" is not evidence, and a vacuous test run is indistinguishable from a real one
- Trigger: a haiku gate agent verifying a batch of money fixes reported "SHIPPABLE — cargo test --all-features -- --ignored: EXIT=0, 10 ignored tests ran, all PASSED (the #[ignore] attributes are stale and should be removed)" and "209 passed". Re-running both commands by hand with DATABASE_URL exported: the ignored run exits 101 with 0 passed and zero lines matching '^test .+ \.\.\. ok$', and the full suite is 513 passed / 0 failed / 10 ignored. Acting on the gate's recommendation would have deleted #[ignore] from ten tests that fail.
- Wrong, and this was MY guidance in the entry above: treating "zero `Skipping` lines in the output" as proof the tests really ran. The skip path is `eprintln!("Skipping ... because DATABASE_URL is not set")`, and libtest CAPTURES stderr and only prints it for FAILING tests unless `--nocapture` is passed. So a run where all 10 files skipped prints zero `Skipping` lines AND reports green — byte-for-byte the same shape as a real run. The check I wrote can never fail.
- Right: the only trustworthy signals are (i) DATABASE_URL confirmed non-empty in the same shell that runs cargo, and (ii) the RUN COUNT compared against a known-good baseline (~513 passed / 0 failed / 10 ignored here; ~209 = lib tests only). For fix-gated tests, invert the assertion: `cargo test --all-features -- --ignored` MUST exit non-zero with 0 passed, and any `... ok` line names a test whose fix has landed and whose `#[ignore]` is now stale.
- Rule: never accept a subagent's verdict on a gate it ran — re-run the load-bearing command yourself when the conclusion decides whether to ship. A verification check that CANNOT FAIL is worse than no check, because it launders a vacuous run into a green report: before trusting any assertion of the form "grep the output for X", confirm X would actually appear on the failure path. And prefer haiku for gathering, never for a ship/no-ship judgement — this gate's own numbers (209 vs the 510 every prior run reported) contradicted its verdict and it did not notice.

## 2026-07-27 — a git commit reported success, echoed its hash, and left NO reflog entry; verify persistence, not exit code
- Trigger: `git commit -q -m ... -- <paths>` in the money-rework worktree returned exit 0 and the very next command in the same shell printed `9d100c628 fix(ledger): ...`. Three commits later, `git log` showed the history jumping straight from the preceding commit to the following one. `git cat-file -t 9d100c628` -> "Not a valid object name"; `git reflog` has NO entry for it; `git merge-base --is-ancestor` says it was never in history. The commit's changes were still intact in the working tree (358 insertions), so nothing was lost — but the commit itself evaporated between two shell invocations.
- Corroborating anomaly on the same volume in the same session: a verification agent found `cargo clippy` reporting `Fresh hotel-app-be` while `rooms_queries.rs`'s mtime was 14+ minutes NEWER than the newest fingerprint file, and had to `cargo clean -p hotel-app-be` to get a truthful build. Both are write/metadata-visibility failures on "/Volumes/APPLE EXTERNAL SSD /".
- Wrong: treating `COMMIT_EXIT=0` plus an echoed hash as proof a commit exists. On this volume it is not. (Root cause UNVERIFIED — unlike 2026-07-26l there was no concurrent session in this isolated worktree, so the concurrent-`git restore` explanation does not apply here.)
- Right: after every commit that matters, assert persistence in a SEPARATE step — `git cat-file -t <sha>` is a commit, `git merge-base --is-ancestor <sha> HEAD`, the sha appears in `git reflog` AND in `git log`, and `git status` is clean. Re-committing the still-intact working tree worked immediately and passed all four checks.
- Rule: on this volume, never chain "commit A, then commit B, then commit C" across separate tool calls and assume A survived — after the last commit, run `git log --oneline -N` and confirm EVERY commit you made is present, by hash. If one is missing, check the working tree first: the content is probably still there and a plain re-commit fixes it. Never conclude work was lost until you have grepped the files for it.
