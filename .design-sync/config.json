{
  "pkg": "hotel-web-fe",
  "projectId": "03f1e68f-8bc7-42c2-97d2-32f663b92470",
  "globalName": "HotelDS",
  "shape": "package",
  "srcDir": "src",
  "entry": "./hotel-web-fe/.ds-entry.tsx",
  "provider": { "component": "AppThemeProvider" },
  "readmeHeader": ".design-sync/conventions.md",
  "guidelinesGlob": ["design-guidelines/**/*.md"],
  "componentSrcMap": {
    "StatCard": "src/components/common/StatCard.tsx",
    "DataTable": "src/components/data-table/DataTable.tsx",
    "TabPanel": "src/components/common/TabPanel.tsx",
    "ModernDatePicker": "src/components/common/ModernDatePicker.tsx",
    "BrandMark": "src/components/common/BrandMark.tsx",
    "LogoLoader": "src/components/common/LogoLoader.tsx"
  },
  "dtsPropsFor": {
    "StatCard": "/** Primary label shown above/below the value. */\n  title: React.ReactNode;\n  /** The headline metric. */\n  value: React.ReactNode;\n  subtitle?: React.ReactNode;\n  /** Icon element rendered in the card's icon slot. */\n  icon?: React.ReactNode;\n  /** Accent color (MUI palette path or CSS color). Defaults to 'primary.main'. */\n  color?: string;\n  /** CSS gradient used when appearance='gradient'. */\n  gradient?: string;\n  appearance?: 'default' | 'gradient';\n  titlePlacement?: 'top' | 'bottom';\n  headerAlignItems?: 'flex-start' | 'center';\n  iconBackground?: string;\n  showPositiveTrendSign?: boolean;\n  subtitleVariant?: string;\n  /** Delta indicator shown with an up/down arrow. */\n  trend?: { value: number; label: string };\n  contentSx?: object;\n  iconSx?: object;\n  titleSx?: object;\n  valueSx?: object;\n  subtitleSx?: object;\n  /** Accepts all MUI CardProps (elevation, variant, onClick, sx, …) except title/color. */\n  [key: string]: unknown;",
    "TabPanel": "children?: React.ReactNode;\n  /** This panel's index; content renders only when value === index. */\n  index: number;\n  /** The currently selected tab index. */\n  value: number;\n  /** Prefix for the generated a11y ids (pair with getTabA11yProps). */\n  idPrefix?: string;\n  className?: string;\n  sx?: object;\n  contentSx?: object;",
    "ModernDatePicker": "/** Field label. */\n  label: string;\n  /** Controlled ISO date value (YYYY-MM-DD). */\n  value: string;\n  onChange: (value: string) => void;\n  required?: boolean;\n  disabled?: boolean;\n  /** ISO date string (YYYY-MM-DD). */\n  minDate?: string;\n  /** ISO date string (YYYY-MM-DD). */\n  maxDate?: string;\n  error?: boolean;\n  helperText?: string;\n  fullWidth?: boolean;\n  size?: 'small' | 'medium';\n  margin?: 'none' | 'dense' | 'normal';",
    "BrandMark": "/** Edge length in px — the mark is square. */\n  size?: number;\n  /** Purely visual use: hide from assistive tech (loaders carry their own status text). */\n  decorative?: boolean;\n  /** Accessible name when not decorative. */\n  title?: string;",
    "LogoLoader": "/** Which loading surface this is. */\n  variant?: 'fullScreen' | 'page' | 'inline' | 'overlay';\n  /** Mark size in px. Defaults: fullScreen 80, page/overlay 48, inline 24. */\n  size?: number;\n  /** Optional visible status line; otherwise a visually-hidden \"Loading\" is announced. */\n  label?: string;\n  /** Entrance delay before the loader fades in. Defaults to 0 on fullScreen, 200 elsewhere. */\n  delayMs?: number;\n  /** Region floor for `page` (default 240px) and `overlay` (default 100%). */\n  minHeight?: number | string;\n  /** Render the mark already settled — skip the draw-on entrance. */\n  skipEntrance?: boolean;\n  sx?: object;",
    "DataTable": "/** Row data. */\n  data: Record<string, unknown>[];\n  /** TanStack Table column definitions (ColumnDef<TData, any>[]). */\n  columns: any[];\n  emptyMessage?: React.ReactNode;\n  /** Global text filter applied across columns. */\n  globalFilter?: string;\n  onRowClick?: (row: Record<string, unknown>) => void;\n  initialSorting?: { id: string; desc: boolean }[];\n  initialColumnFilters?: { id: string; value: unknown }[];\n  pageSize?: number;\n  enablePagination?: boolean;\n  getRowId?: (row: Record<string, unknown>, index: number) => string;"
  }
}
